# 2026-W36 开发周志

## 2026-09-06：综合审阅与文档收口

### 范围

- 项目所有者要求全面审阅，随后授权按审阅意见完善项目文档。
- 审阅基线为 `c52548c`，开始时工作区干净；本轮仅修改文档，不实施代码、样式、素材、Pencil、依赖或部署变更。
- 审阅读取仓库规则、规划、页面与数据、token 与样式、素材治理、构建与验证脚本，并参考已有灰玉桌面和 craft 桌面 / 移动截图。

### 完成情况

- 新建 [官网质量与对外表达收口](../features/site-quality-and-public-content.md)，记录具体问题、证据等级、批次顺序、内容核验字段与完成标准。
- 收敛当前规划，区分本地代码、灰玉首页既有验证、其他页面基线和未核验的线上版本；将内容维护与新增功能的恢复条件分开。
- 修正视觉规范及 UI 附录中的旧 token 版本、星图、主题交互、字体来源、共享 Header 和页面基线描述；对负字距、流式字号与小字对比度记录已知差异，未擅自认定达标或扩大例外。
- 明确构建、静态输出、HTTP 外壳和浏览器检查的不同能力，移除开发规范中尚不存在的 lint / format 执行示例。
- 将 2026-06-13 / 14 部署检查原文归档到 W24，部署专题保留长期配置、SEO 输出限制和发布版本记录要求。
- 为旧页面及视觉 feature 补充适用范围与后续入口，保留历史验证事实；澄清素材 `internal-preview` 表示用途范围而非访问控制。
- 根协作入口没有新增长期启动约束，因此未修改 `AGENTS.md` / `CLAUDE.md`。

### 审阅验证与测量

- `npm run check:local-release` 通过，包含 TypeScript 检查和 Vite 生产构建；检查 29 个公开图片文件与 2 个 Vite 产物。
- 构建输出：JS 259.63 kB（gzip 79.52 kB），CSS 87.86 kB（gzip 14.64 kB），HTML 2.74 kB。此数据不是网络性能或 Core Web Vitals 测量。
- `npm run check:agent-guides` 与 `git diff --check` 在审阅时通过。
- 用模拟 document 执行当前 metadata 函数，确认原为 `#1a1713` 的 `theme-color` 被写成 `#f4efe6`；没有将该定向结果描述为真实浏览器复现。
- 依据 WCAG 相对亮度公式计算：`#8d7b6c` 对 `#f4efe6` 为 3.54:1，对 `#fbf7f0` 为 3.79:1，对 `#efe7db` 为 3.30:1；具体页面仍需结合实际背景复核。
- family-ui 运行时 CSS 与参考副本一致；源码文本扫描未发现未定义的 `--rx-*` / `--rd-*` 引用，此检查不等同于完整 CSS 语义验证。

### 决策与剩余限制

- 继续保留 Vite、React、TypeScript、静态交付与灰玉品牌方向；后续优先质量和公开内容，不以新增页面数量衡量完成。
- 构建期逐页面 HTML、路由事实共享、浏览器测试入口及样式整理均为后续建议，未选择新工具或修改运行时。
- 本次未重跑 HTTP、实时浏览器、外部链接、线上发布版本、搜索收录或兄弟项目最新状态；历史截图仅用于理解既有表现。
- 首页最终验收意见仍需明确；完成文档整理不构成首页最终验收或发布确认。
- 代码问题仍未修复，后续按收口目标逐批实施和记录。

### 文档验证

- `npm run check:agent-guides`、`git diff --check` 通过。
- 对 20 个变更 Markdown 文件执行 UTF-8 / LF / 末尾换行与 80 个本地链接目标检查，通过。
- 核对归档后的部署历史正文与原文件相同，仅调整标题层级。
- 本轮为纯文档变更，未重复执行与改动无关的构建或浏览器检查。

## 2026-09-06：按兄弟产品更新官网规划

- 只读核验十个兄弟仓库的 README、当前状态与必要产品专题，提交锁定来源见 [产品矩阵同步与扩展](../features/product-matrix-refresh.md)。
- 更新现有五项目的内容同步顺位，补充四个独立项目及 Axiom Checker 配套展示建议；不把新仓库数量直接变成首页卡片数量。
- Catalyst 有用户已有未提交的 3D 对照探索；仅记录待复核边界，采用已提交的生产经营方向和视觉验证事实，不改动其工作区。
- 更新当前规划、目标索引、质量收口与素材准备；没有改变实际五项目页面、路由、根入口、素材、域名或部署。
- 本地来源核验不替代远端可读性、产品运行、正式站点和素材授权核验；没有访问或运行兄弟项目业务服务。
- 验证：`npm run check:agent-guides`、`git diff --check` 通过；七个变更 Markdown 文件的 UTF-8 / LF / 末尾换行与 42 个本地链接目标检查通过。本轮仅改文档，未重复执行构建或浏览器检查。
