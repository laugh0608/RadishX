# 2026-W29 开发日志

## 本周目标

- 统一 RadishLex 接入后仍滞留的“四项目 / v1.1”当前口径。
- 完成 v1.2 craft 视觉基线 Phase 3：五个项目详情页；继续完成 Phase 4：Mascot / About。
- 保持路由、文案、素材准入、域名和部署边界不变。

## 完成情况

文档一致性：

- 根 `README.md`、当前规划、功能索引、开发规范和 Vercel 说明统一为五项目、`/lex`、`lex.radishx.com` 与 v1.2 craft 当前事实。
- 项目详情、视觉素材准入、首页 Hero、素材治理、Mascot、About 和 family-ui 相关当前条目同步五项目口径。
- 项目视觉素材准入清单新增 RadishLex `Input pipeline` 当前视觉、候选窗 / manager 截图缺口与隐私审核边界。
- 保留 `docs/devlogs/` 和设计源演进记录中的历史“四项目 / v1.1”表述，不回溯改写历史过程。

craft Phase 3：

- 五种 project tone 增加 `--rx-tone`，驱动详情页 Hero 的项目色点与柔光。
- Hero 状态和 facts 去描边卡片化，改为克制的点状 mono meta。
- 主媒体改无框大圆角柔投影浮层，审核说明从嵌套卡片收敛为弱分隔信息。
- Page flow 改为无框柔投影浮层，四个导览入口去描边并保留 hover / focus。
- 临时项目标识 Hero glyph 去框，继续明确其不是正式 Logo。
- 主题段控按钮补齐 `44×44` 触控目标。

craft Phase 4：

- Mascot Hero、三形态、Gallery、单张表情候选和 Usage 卡片去除页面特有描边，统一为 craft 大圆角、纸面层次和柔投影。
- Mascot 审核状态与 Hero 摘要收敛为带语义色点的 mono meta，弱化嵌套卡片感，同时保留全部素材审核与授权边界。
- About Hero 摘要、微信公众号状态和仓库状态改为点状 meta；联系方式动作、二维码、仓库和域名卡片改为无框柔投影层次。
- Phase 4 仍只修改 CSS 表现层，未改 JSX、数据、路由、文案、素材、域名或部署边界。

## 关键决策

- Phase 3 与 Phase 4 分别实现、验证和提交，保持可分割验收。
- 代码变更限制在站点 CSS 表现层，不改 JSX、路由、数据、文案、素材或 family-ui `--rd-*` 语义。
- 真实截图内部仍允许必要的内容边界；“去框”针对外层容器与卡中卡，不破坏代码内 diagram 的信息结构。
- 线上部署未变化，本轮不重复线上截图级 smoke。

## 验证记录

- `npm run build`：通过（TypeScript + Vite）。
- `git diff --check`：通过。
- Playwright 桌面 `1440×900`：覆盖 `/radish`、`/catalyst`、`/flow`、`/mind`、`/lex`；每页 3 项 Hero facts、4 个 Page flow 入口，无横向溢出或 broken image。
- Playwright 移动 `390×844`：覆盖同五条路由；无横向溢出或 broken image。
- 三态截图抽查：`/flow` 跟随系统桌面、`/flow` 深色桌面、`/lex` 深色移动、`/mind` 浅色移动。
- console error：0。
- 主题段控移动端小触控目标：0。
- QA artifact：`output/playwright/craft-phase3-2026-07-13/`（本地忽略，不提交）。
- `npm run build`：Phase 4 通过（TypeScript + Vite）；`git diff --check` 通过。
- Playwright Phase 4：覆盖 `/mascot`、`/about` 的 `1440×900`、`390×844` 与亮 / 暗主题；无横向溢出、broken image、console error 或小于 `44px` 的交互目标。
- Mascot 保持 3 个形态、7 个 Gallery、10 个单张表情候选、4 个 Usage 分组，22 张图片全部解码；About 保持 4 个联系方式、6 个仓库入口和 7 个域名入口。
- QA artifact：`output/playwright/craft-phase4-2026-07-13/`（本地忽略，不提交）。

## 风险与未完成项

- Phase 5 全站亮色 / 深色 / 跟随系统逐页对照尚未开始。
- 五个项目仍缺正式 Logo、稳定真实截图或公开视频；继续使用已审核图片与代码内临时视觉，不扩大素材边界。
- 当前未部署，本轮视觉结果只代表本地版本。

## 下周建议

1. 按 `docs/features/craft-visual-refresh.md` 进入 Phase 5，全站执行亮色 / 深色 / 跟随系统逐页截图对照与本地发布复核。
2. 只修复终验发现的视觉一致性、响应式或可访问性问题，不扩大页面、素材、路由或部署范围。
3. 没有新项目材料、授权或发布要求时，继续不新增页面、下载入口、seasonal 内容或外部素材。
