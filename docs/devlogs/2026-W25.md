# 2026-W25 开发日志

## 本周目标

- 推进公开素材治理，优先处理已确认的 Mascot 站姿图。
- 将首页中心视觉从可爱Q版安全设定图升级为已确认站姿图。
- 保持首页、Mascot 页、素材文档和当前规划口径一致。
- 复跑本地发布检查，确认关键路由、移动端和公开资源输出仍可用。

## 完成情况

- 生成 3 张站姿素材 Web 版本：
  - `public/images/mascot/radish-child-standing-white-dress-web.jpg`
  - `public/images/mascot/radish-child-standing-white-dress-tall-web.jpg`
  - `public/images/mascot/radish-mature-standing-white-dress-web.jpg`
- 首页 Radish Orbit 中心视觉锚点改用可爱Q版高分辨率站姿图。
- 首页 Mascot 入口改用同一张可爱Q版高分辨率站姿图。
- Mascot 页主视觉和完全体形态卡片改用虚拟形象完全体站姿图。
- 更新首页素材状态槽、Mascot 三形态说明和使用口径文案。
- 调整 Radish Orbit 图区高度和中心卡片尺寸，避免方形站姿图与四个项目节点相交。
- 同步 `docs/assets/materials.md`、`docs/features/` 目标文档、`docs/planning/current.md` 和开发日志索引。
- 本地评估现有 Open Graph 分享预览图，结论为暂不替换，不生成新的 `1200x630` 候选图。
- 补强 RadishMind 详情页代码内自有视觉：从 `Protocol map` 升级为 `Evaluation loop`，表达上下文约束、工具边界、样本评测、证据链和可审计输出。
- 为四个项目补充 RadishX 统一风格代码内临时标识，覆盖首页项目 band、项目详情页 Hero、Public Surface 和矩阵回流卡片。
- 完成四个兄弟项目的 Logo、真实截图和公开视频素材二轮只读复核；未发现新的可接入官网正式页面的项目素材。
- 生成可爱Q版表情格和 4 张贴纸横图 Web 版本，并接入 Mascot Gallery 整图预览。
- 完成 Mascot 单张表情拆分、缩略图命名和下载政策评估，当前继续保持整图预览。
- 完成 `assets/avatars/seasonal/` 6 张节日活动图评估，明确活动 Banner、节日彩蛋和运营内容候选分类；本轮全部不进入当前实现。
- 完成本地发布检查复跑，覆盖构建、空白检查、关键路由、未知路径 fallback、桌面 / 移动端首屏截图、Mascot Gallery 图片加载和 `dist/` 输出文件。
- 修正顶部 `Projects` 导航状态：进入首页 `/#projects` 锚点后，导航 active 状态与四个项目详情页保持一致。
- 为四个项目详情页补充 Hero 下方站内导览，支持跳转到 `Public Surface`、`Documentation`、`Capabilities` 和 `Matrix`。
- 完成一轮站点级可访问性整理：页脚链接容器升级为 `nav`，首页项目入口、星图入口、顶部 / 页脚 GitHub、项目页 Hero 操作和 `InfoRail` 外链补充可访问名称。
- 完成 2026-06-16 设计源、视觉规范、开发规范和 React 实现口径对齐检查；首页、项目页、Mascot 页和 About 页的页面节奏、移动端结构、说明文案和素材边界未发现阻断偏差。
- Pencil 启动后已通过 MCP 补做设计源复核，确认 5 个顶层 v1 画板结构完整且无布局问题；这些早期画板已追加实现口径同步说明 band。
- 通过 Pencil 新增 5 个 v1.1 精确实现稿：首页桌面、首页移动、项目详情模板 Flow 示例、Mascot 页和 About 页；原 v1 画板保留为早期设计记录。
- 完成 React 页面与 v1.1 设计源对照校准：首页项目 band 接入项目详情页同口径媒体，项目详情页 Hero 主媒体、Mascot Hero 和 About 二维码调整首屏加载策略。
- 通过 Pencil 新增 3 个独立 `390px` 移动版 v1.1 画板：项目详情模板 Flow 示例、Mascot 页和 About 页；原桌面 v1.1 画板保留不覆盖。
- 基于新增独立移动稿复查 `/flow`、`/mascot` 和 `/about` 的 `390x844` React 实现；未发现需要立即修改 React 代码的问题。
- 完成本地发布前复查，覆盖构建、`dist/` 输出、关键路由、未知路径 fallback、桌面 / 移动视口、metadata、SEO 文件和公开图片资源。
- 新增 `npm run check:local-release`，把本地发布前静态输出检查固化为可重复运行的脚本。
- 新增 `npm run check:http-smoke`，把本地或未来线上 HTTP 目标检查固化为可重复运行的脚本。
- 完成 2026-06-16 日终文档同步检查，补齐 README、本周提交回顾、目标索引、当前规划和明天事项。
- 完成 About 联系区二轮扫读优化：联系方式补用途标签，微信公众号二维码说明改为公开可读的状态标签，仓库卡片补具体动作文案和可访问名称，首屏补 `No login / API surface` 边界信号。
- 同步 `docs/features/about-contact-surface.md`、`docs/features/README.md`、`docs/planning/current.md` 和本周开发日志，将 About 目标状态更新为二轮扫读优化已完成。
- 完成 Mascot 页 Gallery 信息层级与 Usage 说明实现：7 张整图预览补公开状态、使用边界和后续处理；Usage 说明改为公开展示、安全口径、授权 / 下载和活动图边界四组。
- 同步 `docs/features/mascot-page.md`、`docs/features/README.md`、`docs/planning/current.md` 和本周开发日志，将 Mascot 目标状态更新为 Gallery / Usage 实现已完成；后续已通过 Chrome 插件补跑本地浏览器 smoke。
- 通过 Pencil 同步 `Mascot Page v1.1 - React Accurate` 和 `Mascot Page Mobile v1.1 - React Accurate`，将 Gallery 状态 / 边界 / 后续处理和 Usage 四组边界同步到设计源；本轮不改 React 代码、不新增素材、不推送、不部署。
- 新增 `docs/features/project-visual-asset-readiness.md`，建立四项目正式 Logo、真实截图、视频、key visual、diagram 和 RadishMind 自有视觉的准入字段、当前清单、接入顺序和验证要求。
- 同步 `docs/features/README.md`、`docs/features/project-detail-pages.md`、`docs/features/asset-governance.md`、`docs/assets/materials.md` 和 `docs/planning/current.md`，将四项目视觉素材后续处理从“等待候选”推进为可执行准入流程。
- 用户明确要求使用 Chrome 插件后，补跑本地 `/mascot` 桌面与 `390x844` 移动端 smoke，收束 Gallery facts、Usage 分组、图片加载、横向溢出、触控目标和 console error 检查。
- 完成一轮本地质量基线收束，覆盖生产构建、本地发布静态输出检查和本地 HTTP smoke；本轮不改页面代码、不新增素材、不推送、不部署。
- 完成 Mascot 单张表情拆分准备清单：首批从 `radish-child-expression-sheet-grid.png` 选取 `开心`、`生气`、`震惊`、`得意`、`期待`、`疑问`、`无语`、`OK`、`加油`、`再见` 10 个候选；4 张贴纸横图列为二批复核来源。
- 同步记录拆分审核字段、命名规则、授权边界、使用场景、撤回策略和下载关闭口径；本轮不切图、不生成单张 Web 文件或缩略图、不开放下载入口、不新增 seasonal 实现。
- 进入线上发布准备检查：复跑 `https://radishx.com` 与 `https://www.radishx.com` HTTP smoke 通过，确认线上 8 个关键路由、18 个公开图片资源和 `www` 跳转检查正常。
- 使用 Browser 插件完成线上首页、Flow、Mascot、About 和 404 fallback 默认视口抽查；移动视口截图在 Browser 控制层出现超时，线上移动截图级 smoke 仍待补。
- 按用户要求尝试 Chrome 插件补测线上移动截图：Chrome 默认视口截图可用，但 CDP 移动指标设置、轻量脚本读取和 DevTools 设备模式切换未能稳定进入 `390x844` 移动 viewport。
- 用户手动打开 Chrome 移动端视图后，继续使用 Chrome 插件补读线上首页移动 DOM 指标：确认移动视图宽度为 `321px`，首页 H1、图片解码、broken image 和页面 error console 未发现阻断问题；截图、CDP、可见 DOM、DOM snapshot 和跨路由补查仍出现控制层超时。

## 关键决策

- 可爱Q版高分辨率站姿图作为当前首页中心视觉锚点。
- 虚拟形象完全体站姿图作为 Mascot 页当前主视觉。
- 本轮不替换 `public/images/social/radishx-og-image.png`；现有横图包含 RadishX 标题、项目矩阵信息和 Mascot 视觉，比直接使用方形站姿图更适合作为当前社交预览。
- 站姿图只用于公开展示和品牌识别，不提供下载入口，也不声明可自由复用。
- RadishMind 当前继续使用代码内视觉，不把外部参考截图、未审核图片或概念化 Console 包装成正式产品 UI。
- 四个项目临时标识只作为官网信息识别，不声明为正式 Logo；后续如有独立稳定 Logo，再按素材审核流程替换。
- 二轮项目素材复核后，不把 Godot 默认图标、客户端默认头像、外部产品参考截图、概念 HUD 图、历史 UI 或 archived 项目截图上调为正式 Logo、真实截图或公开视频素材。
- 可爱Q版表情和贴纸当前只作为整图预览，不拆分单张，不提供下载入口；节日图继续只作为活动 Banner、彩蛋或运营候选。
- Mascot 单张拆分会形成可独立传播的衍生素材；未建立拆分清单、命名规则、授权边界和文件包边界前，不生成单张文件、缩略图或下载入口。
- Seasonal 图均带有固定年份、节日祝福或活动场景，当前只保留为短期活动候选；未确认活动窗口、页面位置、文案、授权和撤下策略前，不进入长期 Gallery、默认主视觉或 `public/images/`。
- 本轮可访问性整理只修语义和链接名称，不改变视觉方向、不接入新素材、不触发发布流程。
- 2026-06-16 对齐检查只记录当前实现与规范的一致性，不改变视觉方向、不接入新素材、不触发推送或部署；`.pen` 先补实现口径说明，再新增 v1.1 精确实现稿，原 v1 画板不覆盖。
- v1.1 设计源只使用已审核公开素材或代码内视觉语义；本轮不引入新素材、不开放 Mascot 下载、不把临时项目标识声明为正式 Logo。
- React 与设计源对照校准只修低风险实现差异；首页项目 band 可以使用已审核项目代表图，但不把这些图片上调为正式 Logo、下载素材或上线承诺。
- 项目详情页 Hero 主媒体、Mascot Hero 和 About 二维码属于首屏或近首屏关键信息，使用 eager 加载；Mascot Gallery 和贴纸图继续 lazy，避免一次性加载过多长图。
- 项目详情模板、Mascot 页和 About 页的独立移动版只补设计源，不改 React 实现；移动稿继续沿用已审核公开素材、代码内视觉语义和不下载 / 不 seasonal / 不部署边界。
- 独立移动稿对照复查以页面顺序、关键数量、图片加载、触控目标、横向溢出和 console 为准，不追求设计源与 React 的逐像素一致。
- 本地发布前复查只确认当前本地质量状态，不触发推送、部署、线上 smoke、新素材接入、seasonal 或下载入口。
- 本地发布检查脚本只使用 Node 标准库和现有 `npm run build`，不引入新依赖，也不替代 Browser 桌面 / 移动端视觉 smoke。
- HTTP smoke 脚本只使用 Node 标准库和运行中站点，不引入新依赖；它检查路由 HTML、SEO 文件、公开图片资源和可选 `www` 跳转，不替代截图级视觉 smoke。
- About 二轮只做信息密度、外链上下文、公开页面文案和响应式扫读优化；不新增社交账号、不修改域名策略、不接入新素材、不触发推送或部署。
- 微信公众号二维码在公开页面中继续保留审核状态和使用边界，但不再把内部 Web 文件名作为主要扫读信息展示。
- Mascot 二轮只优化既有 Gallery 和 Usage 说明的信息结构；不新增素材、不替换图片、不拆分单张贴纸、不生成缩略图、不开放下载、不接入 seasonal。
- Gallery 卡片需要把整图预览状态、使用边界和后续处理直接展示出来，避免访问者误解为素材包或下载入口。
- Mascot Gallery / Usage 设计源同步只维护既有 v1.1 桌面与移动画板，不新建第三套画板；移动画板允许加高以完整容纳四组 Usage 边界和页脚。
- 四项目新 Logo、截图、视频或 RadishMind 自有视觉出现前，不替换当前代码内临时项目标识；候选出现后先记录来源、公开状态、替换范围、降级策略和授权备注，再决定是否生成 Web 版本。
- Chrome 插件补跑 `/mascot` smoke 属于本地浏览器验证，不触发发布；线上截图级 smoke 仍放到发布阶段处理。
- 本地质量基线收束只固定当前本地可发布状态，不替代线上 HTTP、根域跳转、路径保留和截图级 smoke；后续只有代码、资源、配置或发布计划变化时再复跑对应检查。
- Mascot 单张表情首批候选只从表情格中选择基础情绪和动作，因为该来源格位较统一；贴纸横图需要额外确认文字方向、社交语境、边缘留白和授权后才能进入单张复核。
- 单张表情计划路径只是审核清单占位，不代表公开文件已经生成；下载政策继续关闭，公开页面不能写成可下载素材、自由使用素材或贴纸包。
- 线上 HTTP smoke 可以作为发布准备的状态层检查；它不替代桌面 / 移动端截图级视觉检查。Browser 插件默认视口抽查可记录为辅助结论，但移动视口控制层超时前，不把线上截图级 smoke 判定为完成。
- Chrome 插件默认视口截图可作为线上视觉辅助抽查，但本轮 Chrome CDP 与脚本读取在移动模拟上不稳定，因此不能替代线上移动截图级 smoke。
- 用户手动打开的 Chrome 移动端视图可用于补充只读 DOM 指标，但只要截图和可见 DOM 通道仍不稳定，就不能把它升级为完整线上移动截图级 smoke。

## 验证记录

- 使用 `sips` 生成站姿素材 Web 版本，确认输出尺寸分别为 `1024x1024`、`1254x1254` 和 `1400x788`。
- 执行 `npm run build` 通过，确认 TypeScript 与 Vite 构建正常。
- 使用 Browser 检查 `/` 在 `1440x900` 与 `390x844` 下的 Radish Orbit，确认中心站姿图加载成功，四个项目节点与中心图无相交，页面无横向溢出，console 无 error。
- 使用 Browser 检查 `/#mascot-entry` 在 `1440x900` 与 `390x844` 下的首页 Mascot 入口，确认站姿图渲染正常，页面无横向溢出，console 无 error。
- 使用 Browser 检查 `/mascot` 在 `1440x900` 与 `390x844` 下的主视觉、三形态和 Gallery，确认新站姿图加载成功，页面无横向溢出，console 无 error。
- 使用本地 Vite 服务检查 3 个新增公开图片资源，均返回 `200` 和 `image/jpeg`。
- 视觉对比 `public/images/social/radishx-og-image.png`、可爱Q版站姿图和完全体站姿图，确认当前 OG 图信息结构更完整，本轮无需替换。
- 执行 `npm run build` 通过，确认 RadishMind `Evaluation loop` 数据结构和样式调整不破坏 TypeScript / Vite 构建。
- 使用 Browser 检查 `/mind` 在 `1440x900` 与 `390x844` 下的 `Evaluation loop`，确认 4 个节点、3 条链路、素材审核说明均渲染，中心层与节点 / 链路不相交，无横向溢出，console 无 error。
- 执行 `npm run build` 与 `git diff --check` 通过，确认四项目临时标识实现不破坏 TypeScript / Vite 构建或空白规范。
- 使用 Browser 检查 `/`、`/radish`、`/catalyst`、`/flow`、`/mind`、`/mascot` 在桌面默认视口与 `390x844` 下的临时项目标识和 Mascot 文案；确认首页 4 个项目 band 标识、项目页 Hero / Public Surface / 矩阵回流卡片均渲染，无横向溢出，console 无 error，Mascot 页不再出现“项目详情页临时 Logo”旧口径。
- 只读复核 `/Users/luobo/Code/Radish`、`/Users/luobo/Code/RadishCatalyst`、`/Users/luobo/Code/RadishFlow`、`/Users/luobo/Code/RadishMind` 的图片、图标和视频候选，确认无新增或修改的媒体文件，未发现 `.mp4`、`.mov`、`.webm`、`.m4v` 或 `.avi` 视频素材。
- 执行 `git diff --check` 通过，确认本轮素材复核文档变更无空白问题；本轮无页面和资源文件改动，未启动浏览器 smoke。
- 使用 `sips` 生成 5 张可爱Q版表情 / 贴纸 Web 版本，输出尺寸为 `933x1400`、`1024x572`、`1376x768`、`1400x788` 和 `1376x768`。
- 执行 `npm run build` 与 `git diff --check` 通过，确认 Mascot Gallery 数据、样式和文档变更不破坏 TypeScript / Vite 构建或空白规范。
- 使用 Browser 检查 `/mascot` 在桌面视口与 `390x844` 下的 Gallery，确认 7 张 Gallery 图片滚动进入视口后均加载成功，无横向溢出，console 无 error。
- 使用本地 Vite 服务检查 5 张新增表情 / 贴纸公开图片资源，均返回 `200` 和 `image/jpeg`。
- Mascot 拆分政策评估为文档类变更，未改页面代码或公开资源；执行 `git diff --check` 验证即可。
- 使用 `sips` 检查 `assets/avatars/seasonal/` 6 张图尺寸：5 张为 `1024x1024`，1 张竖版海报为 `848x1264`。
- 本轮 seasonal 评估只改文档，未改页面代码或公开资源；执行 `git diff --check` 通过。
- 执行 `npm run build` 通过，确认 TypeScript 与 Vite 生产构建正常。
- 执行 `git diff --check` 通过，确认当前改动无空白问题。
- 使用 Browser 检查 `/`、`/radish`、`/catalyst`、`/flow`、`/mind`、`/mascot`、`/about` 和 `/abc-test` 在 `1440x900` 桌面视口下的标题、H1、路由 fallback、横向滚动和 console error；未发现异常。
- 使用 Browser 检查同一组路由在 `390x844` 移动视口下的标题、H1、横向滚动、导航和关键首屏；未发现横向溢出、文字遮挡或按钮挤压。
- 抽查桌面首页、移动首页、移动 Mascot、移动 About 和移动 404 首屏截图，未发现视觉阻断问题。
- 在移动端滚动检查 `/mascot` Gallery，12 张页面图片滚入视口后均完成加载，console error 为空。
- 检查 `dist/` 输出，包含 `index.html`、`favicon.ico`、`robots.txt`、`sitemap.xml`、构建后的 CSS / JS，以及项目、Mascot、社交图片资源。
- 导航状态修正后再次执行 `npm run build` 与 `git diff --check` 通过。
- 使用 Browser 检查 `/#projects` 在 `1440x900` 与 `390x844` 下的锚点滚动和顶部 `Projects` active 状态；确认项目区滚入视口、无横向溢出、console 无 error。
- 使用 Browser 检查 `/radish`、`/catalyst`、`/flow`、`/mind` 在 `1440x900` 与 `390x844` 下的站内导览；确认每页 4 个导览链接可点击到 `Public Surface`、`Documentation`、`Capabilities` 和 `Matrix`，触控高度不低于 `44px`，无横向溢出，console 无 error。
- 站点级可访问性整理后执行 `npm run build` 与 `git diff --check` 通过。
- 使用 Browser 检查 `/`、`/radish`、`/mascot`、`/about` 和 `/abc-test` 在 `1440x900` 与 `390x844` 下的语义结构；确认每页 1 个 `h1`、存在 skip link 与 `main#main-content`、顶部和页脚导航语义正常、图片均有 `alt`、重复链接已具备具体可访问名称、无横向溢出，console 无 error。
- 2026-06-16 对照 `docs/design/visual-guidelines.md`、`docs/development/standards.md`、目标文档、React 页面、数据层和 CSS，复查首页、四个项目页、Mascot 页、About 页的实现口径；未发现需要立即改代码的高风险偏差。
- 2026-06-16 初次尝试通过 Pencil MCP 读取当前设计源状态失败，原因是无法连接正在运行的 Pencil 应用；用户启动 Pencil 后再次读取成功，确认 active editor 为 `docs/design/sources/radishx-site-v0.pen`。
- 2026-06-16 使用 Pencil 检查设计源 5 个顶层画板和变量；确认 `--rx-*` 变量已在设计源中存在，Pencil `snapshot_layout` 报告无布局问题。
- 2026-06-16 使用 Pencil 截图复核首页桌面、首页移动、项目详情模板、Mascot 页和 About 页；结论为结构稳定但视觉内容仍偏 v1 早期候选 / 占位表达。
- 2026-06-16 通过 Pencil 在 5 个顶层画板底部追加 `实现口径同步` 说明 band；更新后再次检查无布局问题。
- 2026-06-16 通过 Pencil 新增 `Home Desktop v1.1 - React Accurate`，修正 RadishMind 媒体说明轻微裁切后，`snapshot_layout` 无布局问题，并完成截图抽查。
- 2026-06-16 通过 Pencil 新增 `Home Mobile v1.1 - React Accurate`，修正移动端 orbit 右侧节点和页面高度后，`snapshot_layout` 无布局问题，并完成截图抽查。
- 2026-06-16 通过 Pencil 新增 `Project Detail Template v1.1 - Flow Example`，增高 Hero 以完整展示媒体审核说明后，`snapshot_layout` 无布局问题，并完成截图抽查。
- 2026-06-16 通过 Pencil 新增 `Mascot Page v1.1 - React Accurate`，收紧页面空白后，`snapshot_layout` 无布局问题，并完成截图抽查。
- 2026-06-16 通过 Pencil 新增 `About Page v1.1 - React Accurate`，收紧页面空白后，`snapshot_layout` 无布局问题，并完成截图抽查。
- 2026-06-16 使用 Browser 检查 `/`、`/radish`、`/catalyst`、`/flow`、`/mind`、`/mascot`、`/about` 和 `/abc-test` 在 `1440x900` 与 `390x844` 下的标题、H1、导航 active 状态、横向滚动、触控目标和 console error；未发现阻断问题。
- 2026-06-16 对首页和 `/mascot` 追加滚动加载复查；Mascot 页 12 张图片滚入视口后均完成加载，首页重复使用的可爱Q版站姿图具备有效原始尺寸，无图片资源 404 或页面 error。
- 2026-06-16 执行 `npm run build` 通过，确认 TypeScript 与 Vite 生产构建正常。
- 2026-06-16 执行 `git diff --check` 通过，确认本轮文档变更无空白问题。
- 2026-06-16 React 对照校准后执行 `npm run build` 通过，确认关键媒体加载属性和首页项目 band 媒体切换不破坏 TypeScript / Vite 构建。
- 2026-06-16 React 对照校准后执行 `git diff --check` 通过。
- 2026-06-16 使用 Browser 检查 `/`、`/flow`、`/mascot`、`/about` 在 `1440x900` 与 `390x844` 下的横向溢出、触控目标、关键图片加载和 console error；未发现横向溢出或 console error。
- 2026-06-16 使用 Browser 滚动检查 `/`、`/flow`、`/mascot`、`/about` 桌面端图片均完成加载；移动端 `/` 项目 band 媒体、`/flow` 主媒体、`/mascot` 主视觉和 `/about` 二维码加载完成。
- 2026-06-16 通过 Pencil 新增 `Project Detail Template Mobile v1.1 - Flow Example`，以 `390px` 单列结构覆盖 `/flow` Hero、Page flow、Public Surface、Documentation、Capabilities、Matrix 和 archived / paused 边界；`snapshot_layout` 无布局问题。
- 2026-06-16 通过 Pencil 新增 `Mascot Page Mobile v1.1 - React Accurate`，以 `390px` 单列结构覆盖完全体站姿主视觉、三形态、7 张 Gallery 整图预览和不拆分 / 不下载 / 不接 seasonal 口径；`snapshot_layout` 无布局问题。
- 2026-06-16 通过 Pencil 新增 `About Page Mobile v1.1 - React Accurate`，以 `390px` 单列结构覆盖联系方式、微信公众号二维码、5 个 GitHub 仓库入口和 6 个域名 / 部署边界；`snapshot_layout` 无布局问题。
- 2026-06-16 本轮只改 Pencil 设计源和文档，未改 React 代码，因此未执行 `npm run build`。
- 2026-06-16 执行 `git diff --check` 通过，确认本轮设计源和文档变更无空白问题。
- 2026-06-16 使用 Browser 检查 `/flow`、`/mascot` 和 `/about` 在 `390x844` 下的独立移动稿对照项：无横向溢出、无低于 `44px` 的可点击目标、console 无 error。
- 2026-06-16 使用 Browser 滚动检查 `/flow`、`/mascot` 和 `/about` 移动端图片加载；`/flow` 2 张、`/mascot` 12 张、`/about` 2 张页面图片全部加载完成。
- 2026-06-16 本轮移动稿对照没有改 React 代码，未执行 `npm run build`；后续如进入本地发布前复查或代码修正，再复跑构建。
- 2026-06-16 执行 `git diff --check` 通过，确认本轮移动稿对照文档变更无空白问题。
- 2026-06-16 本地发布前复查执行 `npm run build` 通过，确认 TypeScript 与 Vite 生产构建正常。
- 2026-06-16 检查 `dist/` 输出，确认包含 `index.html`、`favicon.ico`、`robots.txt`、`sitemap.xml`、构建后的 CSS / JS、3 张项目代表图、13 张 Mascot 图和 2 张社交 / 联系图片。
- 2026-06-16 使用 Browser 检查 `/`、`/radish`、`/catalyst`、`/flow`、`/mind`、`/mascot`、`/about` 和 `/abc-test` 在 `1440x900` 与 `390x844` 下的标题、H1、未知路径 fallback、导航 active 状态、主内容、skip link、横向溢出、触控目标和 console error；未发现阻断问题。
- 2026-06-16 使用 Browser 检查 8 个路由的 metadata、canonical、Open Graph、Twitter Card 和 robots 口径；正常页面为 `index, follow`，未知路径为 `noindex, nofollow`。
- 2026-06-16 使用 Browser 直接打开 `sitemap.xml`、`robots.txt` 和 `public/images` 中 18 个公开图片路径；SEO 文件可访问，18 张图片均可解码并返回有效原始尺寸。
- 2026-06-16 本轮 Browser 后台滚动动作未改变页面 `scrollY`，因此未把 Gallery lazy 图片的页面内滚动触发作为本轮结论；资源解码和页面结构已完成复查。
- 2026-06-16 新增 `scripts/check-local-release.mjs` 和 `npm run check:local-release`，用于检查 `dist/` 入口文件、SEO 文件、Vite asset、sitemap URL、robots sitemap 和公开图片输出。
- 2026-06-16 执行 `npm run check:local-release` 通过；脚本复跑生产构建，并确认 18 个公开图片文件和 2 个 Vite asset 文件已输出。
- 2026-06-16 新增 `scripts/check-http-smoke.mjs` 和 `npm run check:http-smoke`，用于对指定 HTTP 目标检查 8 个关键路由、`robots.txt`、`sitemap.xml` 和 18 个公开图片资源。
- 2026-06-16 执行 `npm run check:http-smoke -- --base-url http://127.0.0.1:4500` 通过；沙箱内本机 HTTP 请求受限，已按权限规则在沙箱外复跑本机目标检查。
- 2026-06-16 执行 `git diff --check` 通过，确认本轮脚本和文档变更无空白问题。
- 2026-06-16 日终文档同步只更新 README、目标索引、About 后续方向、当前规划和开发日志，未改 React 代码；执行 `git diff --check` 验证即可。
- 2026-06-17 About 二轮执行 `npm run build` 通过，确认 TypeScript 与 Vite 生产构建正常。
- 2026-06-17 使用 Browser + CDP 检查 `/about` 在 `1440x900` 下的桌面视口：4 个联系入口、3 个二维码状态标签、5 个仓库卡片、6 个域名卡片均渲染；二维码已解码为 `512x509`；页面 `scrollWidth` 为 1440；无目标元素横向溢出；主内容链接触控目标无小于 `44px`；console error 为空。
- 2026-06-17 使用 Browser + CDP 检查 `/about` 在 `390x844` 下的移动视口：页面 `scrollWidth` 为 390；4 个联系动作宽度均为 308px；二维码已解码；无目标元素横向溢出；主内容链接触控目标无小于 `44px`；console error 为空。
- 2026-06-17 About 二轮执行 `git diff --check` 通过，确认代码和文档变更无空白问题。
- 2026-06-17 About 二轮执行 `npm run check:http-smoke -- --base-url http://127.0.0.1:4500` 通过，确认 8 个路由和 18 个公开图片资源可访问。
- 2026-06-17 Mascot Gallery / Usage 二轮执行 `npm run build` 通过，确认 TypeScript 与 Vite 生产构建正常。
- 2026-06-17 Mascot Gallery / Usage 二轮执行 `git diff --check` 通过，确认代码和文档变更无空白问题。
- 2026-06-17 Mascot Gallery / Usage 二轮执行 `npm run check:http-smoke -- --base-url http://127.0.0.1:4500` 通过，确认 8 个路由和 18 个公开图片资源可访问。
- 2026-06-17 尝试使用 Browser 检查 `/mascot` 桌面与 `390x844` 移动视口时，Browser Use URL policy 拦截本地页面访问；本轮不使用替代浏览器规避该限制，桌面 / 移动截图级或 DOM 级视觉 smoke 待后续可用浏览器会话补跑。
- 2026-06-17 使用 Pencil 更新 `Mascot Page v1.1 - React Accurate`：Gallery 说明和卡片说明补齐公开状态、使用边界和后续处理，Governance Summary 调整为 Status、Boundary、Next 和 Seasonal 四项；`snapshot_layout` 无布局问题。
- 2026-06-17 使用 Pencil 更新 `Mascot Page Mobile v1.1 - React Accurate`：Gallery 卡片说明补齐状态 / 边界摘要，Usage 从三条规则调整为 Display、Safety、License 和 Seasonal 四组；移动画板高度从 `4300px` 调整为 `4500px` 后，`snapshot_layout` 无布局问题。
- 2026-06-17 四项目视觉素材准入准备为纯文档治理变更，未改页面代码、未新增公开资源、未推送、未部署；执行 `git diff --check` 验证即可。
- 2026-06-17 使用 Chrome 插件检查本地 `http://127.0.0.1:4500/mascot`：`1440x900` 下 7 张 Gallery 卡片、7 个状态标签、14 条 Boundary / Next facts、4 个 Gallery notes、4 个 Usage 分组和 11 张页面图片均正常；`390x844` 下同项检查正常，11 张图片滚动后全部完成解码，无横向溢出、无小于 `44px` 的可点击目标，console error 为空。
- 2026-06-17 已通过 Chrome 截图抽查桌面 Gallery 和移动 Usage 视口，未发现文字遮挡、卡片错位或下载入口误导。
- 2026-06-17 本地质量基线收束执行 `npm run build` 通过，确认 TypeScript 与 Vite 生产构建正常。
- 2026-06-17 本地质量基线收束执行 `npm run check:local-release` 通过；脚本复跑生产构建，并确认 18 个公开图片文件和 2 个 Vite asset 文件已输出。
- 2026-06-17 临时启动 `http://127.0.0.1:4500/` 后，执行 `npm run check:http-smoke -- --base-url http://127.0.0.1:4500` 通过，确认 8 个关键路由和 18 个公开图片资源可访问；检查后已关闭本地服务。
- 2026-06-17 Mascot 单张表情拆分准备清单为纯文档治理变更，未改 React 代码、未新增公开资源、未生成单张 Web 文件或缩略图；执行 `git diff --check` 通过。
- 2026-06-17 线上发布准备执行 `npm run check:http-smoke -- --base-url https://radishx.com --www-url https://www.radishx.com` 通过，确认 8 个线上关键路由、18 个公开图片资源和 `www` 到 canonical 根域入口检查正常。
- 2026-06-17 使用 Browser 插件检查线上 `https://radishx.com/` 默认视口：标题为 `RadishX - Radish 系列项目矩阵`，首屏截图可见首页 Hero、Radish Orbit、中心 Mascot 视觉和顶部导航，未发现首屏视觉阻断。
- 2026-06-17 使用 Browser 插件检查线上 `https://radishx.com/flow` 默认视口：标题为 `RadishFlow - RadishX`，首屏截图可见项目 Hero、archived 状态、历史 UI baseline、未来域名和仓库入口，未发现首屏视觉阻断。
- 2026-06-17 使用 Browser 插件检查线上 `https://radishx.com/mascot` 默认视口：标题为 `萝小白 - RadishX`，首屏截图可见 Mascot Hero、主视觉图和顶部导航 active 状态；DOM 指标显示 `scrollWidth` 与 `clientWidth` 均为 `1280`，无横向溢出，broken image 为空，页面相关 console error 为空。
- 2026-06-17 使用 Browser 插件检查线上 `https://radishx.com/about` 默认视口：标题为 `About - RadishX`，首屏截图可见 About Hero、公开边界说明、状态标签和顶部导航 active 状态，未发现首屏视觉阻断。
- 2026-06-17 使用 Browser 插件检查线上 `https://radishx.com/abc-test` 默认视口：标题为 `页面不存在 - RadishX`，截图可见 404 fallback、回到首页入口和页脚导航，fallback 视觉正常。
- 2026-06-17 Browser 插件在 `tab.goto()` 跨路由导航中偶发等待较长，`390x844` 视口下 `Page.captureScreenshot` 对完整截图和小范围 clip 均出现控制层超时；期间 `ab.chatgpt.com` Statsig 请求超时来自 Codex / Browser 插件环境，不是 `radishx.com` 页面错误。线上移动截图级 smoke 仍待补。
- 2026-06-17 使用 Chrome 插件检查线上 `https://radishx.com/` 默认视口：Chrome extension 轻量通信正常，可列出当前 Chrome tab；默认视口截图可见首页 Hero、Radish Orbit、中心 Mascot 视觉和顶部导航。
- 2026-06-17 使用 Chrome 插件尝试线上移动补测：通过 CDP 执行 `Emulation.setDeviceMetricsOverride` 设置 `390x844` 时出现控制层超时；随后轻量 `Runtime.evaluate` 和 Chrome Playwright `evaluate` 读取宽度也超时；尝试通过 Chrome 快捷键打开 DevTools 设备模式后再次截图，页面仍保持桌面布局。
- 2026-06-17 用户手动在 Chrome 中打开线上移动端视图后，使用 Chrome 插件认领 `https://radishx.com/` 标签页并读取首页 DOM 指标：`innerWidth`、`clientWidth` 和 `scrollWidth` 均为 `321`，`devicePixelRatio` 为 `2`，H1 为 `Radish 系列项目矩阵`，6 张页面图片中 5 张已完成解码，broken image 为空，页面 error console 为空。
- 2026-06-17 同一 Chrome 手动移动视图下，`tab.screenshot()`、CDP `Page.getLayoutMetrics`、CDP `Page.captureScreenshot`、`dom_cua.get_visible_dom()` 和 `domSnapshot()` 仍出现控制层超时；尝试跨关键路由批量补查时工具层超时，重连后标签页停在 `/flow`，后续 `/flow` 轻量指标和 console 读取也超时。

## 风险与未完成项

- Open Graph 分享预览图沿用完全体设定图版本；后续只有首页视觉再次明显变化或社交预览目标变化时再评估替换。
- Mascot 单张表情拆分准备清单和首批候选已建立，但尚未进入人工框选、授权复核、单张文件生成或页面接入；seasonal 图已完成分类评估，但具体活动目标和下载口径仍未确认。
- 四个项目仍缺独立稳定 Logo、RadishMind 自有公开图片和可公开视频素材；本轮复核后继续等待新的候选来源。
- 四项目视觉素材准入清单已建立，但仍需要等待真实候选素材来源；没有候选来源时不主动迁入兄弟项目资源。
- 本地发布检查、本地质量基线、本地 HTTP smoke 和线上 HTTP smoke 已复跑；线上桌面 / 移动端完整截图级 smoke 仍待补。
- 设计源已补齐项目详情模板、Mascot 页和 About 页的独立移动稿，且 Mascot Gallery / Usage 已同步到二轮实现口径；后续如果页面继续改版，需要同步维护桌面与移动设计源。
- Browser 截图接口本轮在移动视口控制上出现超时；Chrome 插件默认截图可用但移动模拟和手动移动视图截图通道仍未收束；`/mascot` 本地 smoke 已通过 Chrome 插件补测，线上首页移动 DOM 指标和首页、Flow、Mascot、About、404 默认视口已有辅助抽查，完整线上移动截图级 smoke 仍待补。

## 提交回顾

2026-06-16 共完成 8 个本地提交，不含本次日终文档收束提交：

1. `9aedede docs: record design source alignment`：记录设计源、视觉规范、开发规范和 React 实现口径对齐检查。
2. `4a50ac5 docs: add design source v1.1 plan`：新增 Pencil v1.1 精确实现稿目标文档和执行计划。
3. `d7b0302 feat: align react pages with design source`：校准首页项目 band、关键媒体加载策略和 v1.1 设计源口径。
4. `4edda7a docs: add mobile pencil boards`：补齐项目详情模板、Mascot 页和 About 页独立 `390px` 移动版 v1.1 画板。
5. `959cd0d docs: record mobile design alignment`：记录独立移动稿与 `/flow`、`/mascot`、`/about` React 实现对照复查。
6. `057dcf7 docs: record local release review`：记录本地发布前复查，覆盖构建、关键路由、metadata、SEO 文件和公开图片资源。
7. `5ba8439 build: add local release check`：新增 `npm run check:local-release`，固化生产构建输出检查。
8. `37f35d1 test: add http smoke check`：新增 `npm run check:http-smoke`，固化本地或未来线上 HTTP 目标检查。

2026-06-15 共完成 12 个本地提交：

1. `aee46b7 feat: add mascot standing visuals`：接入 Mascot 站姿 Web 版本和首页 / Mascot 页使用口径。
2. `9536c77 docs: record og preview decision`：记录 Open Graph 图暂不替换结论。
3. `868597a feat: enhance radishmind visual`：将 RadishMind 详情页代码内视觉升级为 `Evaluation loop`。
4. `bbb0a70 feat: add temporary project marks`：为四个项目接入代码内临时项目标识。
5. `188ee15 docs: record project asset audit refresh`：记录四个兄弟项目素材二轮只读复核。
6. `4768431 feat: expand mascot gallery stickers`：接入可爱Q版表情格和贴纸横图整图预览。
7. `7d787e8 docs: record mascot sticker policy evaluation`：记录单张拆分、缩略图命名和下载政策评估。
8. `5075220 docs: evaluate mascot seasonal assets`：记录 seasonal 活动图分类、窗口、位置、文案和授权边界。
9. `b09c067 docs: record local release smoke`：记录本地发布检查复跑结果。
10. `5f65c50 fix: stabilize projects nav state`：修正 `/#projects` active 状态。
11. `bdc1a11 feat: add project page section navigation`：补充四个项目详情页站内导览。
12. `e8759ac fix: improve site accessibility labels`：补强全站语义结构和重复链接可访问名称。

同步检查：

- `docs/features/design-source-refresh.md` 已记录首页桌面 / 移动、项目详情模板、Mascot 页、About 页、三张独立移动版 v1.1 设计源，以及 2026-06-17 Mascot Gallery / Usage 设计源同步。
- `docs/features/react-design-alignment.md` 已记录 React 与 v1.1 设计源对照校准，以及三页独立移动稿对照结果。
- `docs/features/visual-qa-and-release.md`、`docs/development/standards.md` 和 `package.json` 已同步本地发布检查、HTTP smoke 命令、Mascot Chrome smoke、本地质量基线和线上 HTTP smoke 记录。
- `docs/features/README.md`、`docs/planning/current.md` 和本周开发日志已更新到 2026-06-17 About、Mascot 二轮和 Mascot 设计源同步口径；根 README 保持现有入口状态，本轮不需要同步修改。
- `docs/features/project-visual-asset-readiness.md`、`docs/features/project-detail-pages.md`、`docs/features/asset-governance.md` 和 `docs/assets/materials.md` 已同步四项目视觉素材准入字段、接入顺序和当前不替换结论。
- `docs/assets/materials.md` 已记录 Mascot、项目代表图、贴纸、seasonal 和下载边界。
- `docs/features/mascot-page.md`、`docs/features/asset-governance.md`、`docs/assets/materials.md`、`docs/features/README.md` 和 `docs/planning/current.md` 已同步 Mascot 单张表情拆分准备清单、首批候选、命名规则、授权边界、使用场景、撤回策略和下载关闭口径。
- `AGENTS.md` 与 `CLAUDE.md` 本轮无通用协作规则、执行边界或稳定入口变化，不需要同步修改。

## 明天事项

1. 继续先不推送、不部署；线上 HTTP smoke 已通过，如果确认进入完整发布阶段，再按 `docs/features/visual-qa-and-release.md` 补跑线上桌面 / 移动端截图级 smoke。
2. 本地质量基线已收束，后续只有代码、资源、配置或发布计划变化时再复跑对应检查。
3. 后续设计侧只在 React 页面继续调整或新增正式素材时再同步对应 v1.1 画板；涉及 `.pen` 设计源时只通过 Pencil 工具处理。
4. 不启动 seasonal 页面实现，除非先确认具体活动目标、上线 / 下线时间、页面位置、文案边界和授权来源。
5. 四个项目素材后续只在出现新的独立 Logo、真实截图、公开视频或 RadishMind 自有视觉候选来源时按准入清单重新审核；不主动迁入兄弟项目素材。
6. Mascot 单张表情后续只有进入人工框选、授权复核或页面展示时，才基于准备清单生成文件并补做对应视觉检查。

## 下周建议

1. 如果后续进入部署阶段，复用 HTTP smoke 脚本确认线上状态层，并补齐桌面 / 移动端截图级 smoke。
2. 如果后续选择具体 seasonal 活动，先确认活动目标、上线 / 下线时间、页面位置、文案、授权和撤下策略，再生成 Web 版本。
3. 四个项目出现新的独立 Logo、真实截图、视频或 RadishMind 自有视觉候选来源后，先按准入清单补齐字段，再进入项目素材审核和 Web 优化。
