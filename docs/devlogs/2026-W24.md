# 2026-W24 开发日志

## 本周目标

- 明确 RadishX 官网定位、技术栈和页面结构。
- 整理虚拟形象和社交媒体素材。
- 建立基础文档、协作文件和仓库通用配置。

## 完成情况

- 确认技术栈为 Vite + React + TypeScript。
- 确认官网部署在 Vercel，主域名为 `radishx.com`。
- 完成 Vercel 首次线上 smoke，确认 `https://www.radishx.com/` 可访问，站内路径和未知路径均回退到 `index.html`。
- 确认仓库名为 `RadishX`，公开仓库为 `laugh0608/RadishX`。
- 确认页面范围：首页、四个项目介绍页、Mascot、About。
- 确认四个未来项目域名：`hub`、`forge`、`flow`、`mind`。
- 整理 20 张虚拟形象图片到 `assets/avatars/`。
- 整理微信公众号二维码到 `assets/social/`。
- 新增“萝小白”可爱Q版安全设定图候选 `assets/avatars/child/radish-child-safe-design-sheet-v1.png`。
- 新增 `LICENSE`，采用与四个兄弟项目一致的 source-available 许可证口径。
- 新增 `docs/` 文档骨架。
- 新增 `AGENTS.md`、`CLAUDE.md`、`.gitignore`、`.editorconfig`、`.gitattributes`。
- 新增 RadishX Pencil 设计源文件 `docs/design/sources/radishx-site-v0.pen`。
- 扩充 `docs/design/visual-guidelines.md`，形成 RadishX 视觉与前端规范。
- 扩充 `docs/development/standards.md`，形成 Pencil-first 开发流程、项目结构、路由、样式和验证规则。
- 在 Pencil 中尝试过早期首页低保真稿；评审后废弃，并已从 `docs/design/sources/radishx-site-v0.pen` 删除。
- 根据评审反馈追加首页桌面版 v1 `Home Desktop v1 - Radish Orbit`，改为 RadishX 自有的项目星图、四道入口和 Mascot 叙事结构。
- 在 Pencil 中完成首页移动版 `Home Mobile v1 - Radish Orbit`，将桌面星图压缩为移动端可读的项目轨道、入口列表和 Mascot 收束区。
- 在 Pencil 中完成项目详情页模板 `Project Detail Template v1 - Flow Example`，用于四个项目详情页复用。
- 在 Pencil 中完成 `Mascot Page v1`，包含“萝小白”三形态总览、素材 Gallery 和可爱Q版安全候选说明。
- 在 Pencil 中完成 `About Page v1`，包含联系方式、微信公众号二维码占位、五个 GitHub 仓库入口和部署边界说明。
- 确认第一版项目详情页暂不放真实截图和视频素材，保留设计稿中的候选媒体框和项目状态信息。
- 使用萝小白原始形象生成 `public/favicon.ico`，作为官网站点图标和第一版临时 Logo 来源。
- 初始化 Vite + React + TypeScript 官网项目。
- 新增 `package.json`、`package-lock.json`、`tsconfig.json`、`vite.config.ts` 和 `index.html`。
- 配置 Vite 本地开发服务器默认监听 `127.0.0.1:4500`。
- 按开发规范建立 `src/app`、`src/components`、`src/data`、`src/pages` 和 `src/styles` 目录。
- 实现轻量站内路由，覆盖 `/`、`/radish`、`/catalyst`、`/flow`、`/mind`、`/mascot` 和 `/about`。
- 接入项目、导航、联系方式和 Mascot 结构化数据层。
- 接入 `public/favicon.ico` 作为站点图标、导航图标和第一版临时 Logo。
- 落地 `--rx-*` 样式 token、全局样式和响应式布局骨架。
- 实现首页、四个项目详情页、Mascot 页、About 页和 404 页面首版静态官网骨架。
- 添加 `vercel.json`，为官网站内 History API 路由提供 `index.html` fallback，不配置四个未来项目域名 rewrite。
- 新增通用 `InfoRail` 信息栏组件，用于展示站点、路由、仓库和未来域名等稳定公开信息。
- 补强首页 Open Signals 区，明确当前站点、公开仓库和四个未来域名不作为当前 Vercel 路由。
- 补强四个项目详情页的 Public Surface 区，明确第一版只开放站内介绍页、GitHub 仓库和未来域名状态。
- 补强 Mascot 页使用规则区，记录“萝小白”公开素材需要先过审核，第一版仅保留安全口径说明。
- 补强 About 页域名和部署边界区，集中展示 `radishx.com` 与四个未来项目域名的边界。
- 细化首页项目 band，补充未来域名、项目角色和三条公开信号，使首页项目矩阵更接近 Pencil v1 的大幅横向信息节奏。
- 细化图形化候选媒体框，补充项目短名、阶段和项目轨道标签，继续避免使用未审核截图或视频素材。
- 调整全局标题字号实现，不再使用 `vw` 缩放，改为明确桌面、平板和移动端字号层级。
- 细化 Mascot 页移动端节奏：三形态增加独立状态块、审核备注和 390px 两列小密度布局，继续不公开未审核候选大图。
- 细化 About 页移动端信息层级：联系方式支持长文本换行，微信公众号二维码保留占位和素材路径口径，仓库入口与域名边界改为更可扫读的单列卡片。
- 细化四个项目详情页移动端信息层级：Hero 增加站内路径、未来域名和仓库摘要，媒体框明确第一版不展示真实截图或视频，Public Surface 增加视觉占位状态。
- 细化首页主视觉资产接入口径：保留 Radish Orbit 项目星图作为首版 Hero 主结构，并新增 Hero 主视觉、萝小白三形态和项目媒体三类素材状态槽。
- 补齐官网第一版上线前基础 metadata：页面标题、描述、canonical、Open Graph、Twitter Card、404 noindex、skip link 和外链安全口径。
- 回顾 2026-06-13 全部提交，确认设计规范、部署说明、当前规划和开发日志需要同步部署状态、线上 smoke 结果和下一步安排。
- 同步 `README.md`、`AGENTS.md` 与 `CLAUDE.md`，记录当前可访问域名和必要验证口径。
- 2026-06-14 复跑本地同版本桌面与移动端全路由响应式 smoke，覆盖 `/`、四个项目详情页、`/mascot`、`/about` 和 `/abc-test`。
- 修正全站导航、页脚和信息栏链接的触控目标尺寸，使短标签链接也具备不小于 `44px` 的实际点击区域。
- 放宽 About 页二维码占位侧栏宽度，修复桌面视口下长素材路径说明的文本溢出。
- 2026-06-14 复查 `https://www.radishx.com/` 线上 HTTP 状态，首页、站内路径和未知路径均返回 `200`。
- 确认 `radishx.com` 为官网 canonical 主域，`www.radishx.com` 作为兼容入口跳转到根域。
- 添加 `public/sitemap.xml` 和 `public/robots.txt`，覆盖当前官网站内页面，并更新 Vercel rewrite 排除规则。
- 完成首批公开素材审核：确认可爱Q版安全设定图、完全体设定图、原始图标、服装变体图、完全体表情包图和微信公众号二维码可进入第一版官网。
- 生成首批 Web 优化图片到 `public/images/mascot/` 与 `public/images/social/`，保留 `assets/` 原始素材不覆盖。
- 接入首页 Mascot 辅助视觉、Mascot 页主视觉 / 三形态 / Gallery 图片和 About 页微信公众号二维码。
- 生成 1200x630 Open Graph 分享预览图 `public/images/social/radishx-og-image.png`，并将 Open Graph / Twitter Card 升级为 `summary_large_image`。
- 完成一轮页面可访问性与生产质量检查：修正移动端导航触控高度、减少动效偏好下的站内滚动行为、站内换页后的正文焦点转移，以及首批素材接入后的过期文案。
- 完成四个项目详情页独立素材只读审核：Radish 确认 README 角色图，RadishCatalyst 确认概念视觉，RadishFlow 确认 Studio UI baseline，RadishMind 仅发现外部产品参考截图，暂不接入正式图。
- 生成 `public/images/projects/radish/radish-acg-web.jpg`、`public/images/projects/catalyst/radishcatalyst-rpg-exploration-concept-web.jpg` 和 `public/images/projects/flow/radishflow-workbench-concept-web.jpg`，保留兄弟项目原始素材不覆盖。
- 为项目详情页主媒体区接入审核图元数据，首页 compact 项目 band 继续使用原图形化轨道框，避免本轮素材替换扩散到首页节奏。
- 将 RadishFlow 页面状态收敛为 `Archived`，说明 UI baseline 只作为历史视觉方向展示，不代表继续公开维护、产品化或交付承诺。
- 为 RadishMind 详情页补代码内 `Protocol map` 视觉，用协议、评测、工具编排和审计四组公开信号替换通用占位，不引入外部参考截图或未审核图片。

## 关键决策

- `x.radishx.com` 不直接作为 Radish 本体入口，保留给实验、短链或统一跳转页。
- 四个项目未来域名不是当前官网的 Vercel 路由。
- 首页风格偏创意品牌、游戏感和视觉冲击，但保留 Radish 的温润、克制、纸感和低饱和视觉 DNA。
- 第一版项目详情页优先开放 GitHub 链接，未稳定上线的 Demo、下载页或独立项目站点不强行展示。
- “萝小白”可爱Q版官网素材必须使用完整外装、非性感化、无内衣展示、无身体拆解的安全版本。
- 官网代码、文档和原创素材使用 source-available 口径，第三方内容遵循其各自许可证。
- 官网设计采用 Pencil-first 流程：先在 `.pen` 中确认首页、项目模板、Mascot 和 About，再进入 React 实现。
- RadishX 视觉基线吸收 Radish 淡雅新中式、纸感、印色感和低饱和配色，并参考 MiMo Code 的现代东方页面节奏，但不复制其页面、图标、配色或文案。
- 早期首页低保真稿被判定过于贴近 MiMo Code 的结构，已从 Pencil 源文件删除；后续只以 v1 的 Radish Orbit 方向继续推进，只保留参考页的纸感、轻纹样、柔和浮层和滚动节奏。
- 四个项目详情页第一版不强求真实截图或视频，先保证定位、状态、GitHub、未来域名和项目关系清楚。
- 官网站点图标和项目临时 Logo 可使用萝小白原始形象；未来项目有独立 Logo 后再替换。
- 第一版 React 路由不引入 `react-router-dom`，先用轻量路由表和 History API 保持静态官网依赖收敛。
- 第一版公开页面不使用未审核候选大图；Mascot 和 About 中与未确认素材相关的位置先展示审核状态或图形化占位。
- 首页主视觉在 React 首版中先承载素材审核状态和替换边界，正式角色图、项目截图和视频素材仍需审核后再进入 `public/images/`。
- 分享预览使用已确认素材生成的 1200x630 PNG，并通过 `og:image`、`twitter:image` 和 `summary_large_image` 输出。
- `radishx.com` 是官网 canonical 主域，`www.radishx.com` 作为兼容入口跳转到根域。
- 首批 Mascot 素材可以进入公开页面，但不提供下载入口，也不声明可自由复用；后续新增素材仍需先在 `assets/` 审核，再生成 `public/images/` Web 版本。
- 项目详情页素材可以按“代表性图片 / 概念视觉 / UI baseline”分级接入，但必须在页面和素材文档中说明限制；概念图不当作实机截图，UI baseline 不当作产品可用承诺。
- RadishCatalyst `client/icon.svg` 是 Godot 默认图标，不作为官网项目 Logo；RadishMind 当前只有外部参考截图，不能进入 `public/images/projects/`。
- RadishFlow 兄弟仓库已声明 2026-06-12 起停止公开维护，官网详情页必须避免把截图或未来域名包装成继续迭代、下载或上线承诺。
- RadishMind 在没有自有图片素材前，可以使用代码内结构化视觉表达项目定位；这不等同于公开截图审核通过，也不占用 `public/images/projects/`。

## 今日提交回顾（2026-06-13）

- `ac96d90 docs: update RadishX pencil design direction`：建立并同步 Pencil-first 设计方向。
- `35b3761 chore: confirm initial site assets`：确认初始素材和 favicon 来源。
- `271f558 feat: initialize React site`：初始化 Vite + React + TypeScript 官网骨架。
- `1964c28 feat: clarify public site surfaces`：补强公开页面边界、仓库入口和未来域名说明。
- `93d5d55 feat: refine project matrix bands`：细化首页项目矩阵 band。
- `e478953 feat: refine mascot and about mobile pages`：细化 Mascot 与 About 的 390px 移动端可读性。
- `08b9279 feat: refine project detail responsive surfaces`：细化四个项目详情页响应式信息层级。
- `3b933bd docs: record tablet and wide responsive QA`：记录 768px tablet 与 1728px wide QA。
- `69839e2 feat: refine home visual asset states`：补充首页主视觉、萝小白和项目媒体素材状态槽。
- `55f7cec feat: add site metadata foundation`：补齐 metadata、canonical、社交分享、404 noindex、skip link 和外链安全口径。
- 本次收尾文档提交用于同步部署 smoke、文档口径和 2026-06-14 明天事项。

## 验证记录

- 检查根目录图片数量和类型。
- 检查素材哈希，没有发现字节级完全重复文件。
- 检查整理后的素材目录和 README 记录。
- 检查新增文档和通用配置文件。
- 检查生成的可爱Q版安全设定图，确认没有内衣、身体拆解或暴露性设定。
- 检查 Pencil active editor 路径，确认设计源文件位于 RadishX 仓库。
- 检查视觉与开发规范文档差异。
- 检查首页桌面版 v1 的 Pencil layout，无裁切、坍塌或溢出问题。
- 检查首页移动版 `Home Mobile v1 - Radish Orbit` 的 Pencil layout，无裁切、坍塌或溢出问题，并导出 PNG 预览检查中文换行和候选素材块。
- 检查项目详情页模板 `Project Detail Template v1 - Flow Example` 的 Pencil layout，无裁切、坍塌或溢出问题，并导出 PNG 预览检查状态、未来域名和 GitHub 入口层级。
- 检查 `Mascot Page v1` 的 Pencil layout，无裁切、坍塌或溢出问题，并导出 PNG 预览检查三形态、Gallery 和安全口径区域。
- 检查 `About Page v1` 的 Pencil layout，无裁切、坍塌或溢出问题，并导出 PNG 预览检查联系方式、仓库入口和部署边界。
- 检查 Pencil 顶层画布，确认废弃的早期首页低保真稿已删除，当前只保留 v1 方向和后续页面稿。
- 生成并检查 `public/favicon.ico`，来源为 `assets/avatars/origin/radish-origin-icon.jpg`。
- 使用 npm 安装 React、Vite、TypeScript 和类型依赖，生成 `package-lock.json`；`npm audit` 检查为 0 个漏洞。
- 执行 `npm run type-check` 通过。
- 执行 `npm run build` 通过，输出目录为 `dist/`。
- 执行 `npm run dev`，确认 Vite 默认监听 `http://127.0.0.1:4500/`。
- 启动本地开发服务器，在 Chrome 中检查桌面首页、7 个站内路由、移动 390px 首页、项目区和 RadishFlow 详情页。
- Chrome 桌面路由检查确认 `/`、四个项目页、`/mascot`、`/about` 均无横向溢出、无 error console。
- Chrome 移动 390px iframe 检查确认 7 个站内路由均能渲染对应 H1、导航和关键区域，无 error console。
- 调整首页桌面项目星图底部节点位置，修复 RadishFlow 与 RadishMind 节点在 Chrome 1318px 宽度下的重叠。
- 使用 Chrome 插件真实联调 `localhost:4500`，检查 `/`、`/flow`、`/mascot`、`/about` 桌面路由，确认无横向溢出、无页面 error console。
- 使用 Chrome 插件和本地开发服务器 smoke 入口检查 `390px` 与 `1440px` 下的首页、Flow 状态区、About 域名区和 Mascot 使用规则区，确认关键区域均渲染。
- 使用 Chrome 插件截图检查 About 域名区和 Flow 移动状态区，确认 hash 目标在稳定状态下可滚动到对应区域，信息栏和域名卡片可读。
- 执行 `npm run type-check` 通过。
- 执行 `npm run build` 通过。
- 使用 Chrome 插件真实联调 `localhost:4500`，检查首页和 Flow 详情页桌面路由，确认无横向溢出、无页面 error console。
- 使用 Chrome 插件和本地开发服务器 smoke 入口检查 `390px` 首页项目区和 Flow 详情页，确认项目 band、未来域名、公开信号和媒体框标签均渲染。
- 使用 Chrome 插件截图检查首页项目区桌面和 `390px` 移动视图，确认新增信息层级可读，按钮没有挤压。
- 执行 `npm run type-check` 通过。
- 执行 `npm run build` 通过，输出目录为 `dist/`。
- 使用 Chrome 插件真实联调 `localhost:4500`，检查 `/`、`/mascot`、`/about` 桌面路由，确认无横向溢出、无页面 error console。
- 使用 Chrome 插件和临时 390px iframe QA 页面检查 `/mascot` 与 `/about`，确认三形态、4 个联系方式、5 个 GitHub 仓库入口、5 个域名卡片和二维码占位均可检测，`scrollWidth` 为 390 且无横向溢出；临时 QA 文件已删除。
- 执行 `npm run type-check` 通过。
- 执行 `npm run build` 通过，输出目录为 `dist/`。
- 使用 Chrome 插件真实联调 `localhost:4500`，检查 `/radish`、`/catalyst`、`/flow`、`/mind` 桌面路由，确认均有 3 个 Hero 摘要、4 行 Public Surface 信息、1 个禁用访问入口、媒体框说明和 3 个矩阵回流入口，无横向溢出、无页面 error console。
- 使用 Chrome 插件和临时 390px iframe QA 页面检查四个项目详情页 Hero、媒体框和状态区，确认 `scrollWidth` 为 390，新增信息层级可检测且无横向溢出；临时 QA 文件已删除。
- 使用 Chrome 插件和临时响应式 QA 页面检查 `/`、四个项目详情页、`/mascot`、`/about` 在 `768x1024` 与 `1728x1117` 下的关键区域计数、`scrollWidth` 和横向溢出，确认无失败项、无页面 error console；临时 QA 文件已删除。
- 执行 `npm run type-check` 通过。
- 执行 `npm run build` 通过，输出目录为 `dist/`。
- 使用 Chrome 插件真实联调 `localhost:4500`，检查首页桌面 Hero，确认 4 个项目节点、3 个素材状态槽均渲染，`scrollWidth` 与视口一致，无横向溢出、无页面 error console。
- 使用 Chrome 插件和临时 390px iframe QA 页面检查首页移动视口，确认 Hero、Radish Orbit、3 个素材状态槽均渲染，`scrollWidth` 为 390，项目节点和素材槽无横向溢出；临时 QA 文件已删除。
- 执行 `npm run type-check` 通过。
- 执行 `npm run build` 通过，输出目录为 `dist/`。
- 使用 Chrome 插件真实联调 `localhost:4500`，检查 `/`、`/radish`、`/flow`、`/mascot`、`/about` 和 404 测试路径，确认 title、description、canonical、Open Graph、Twitter Card、robots、skip link 和外链 `rel` 口径均可检测，站内页面无来源于 `127.0.0.1:4500` 的 error console。
- 使用 Chrome 插件和临时 390px iframe QA 页面检查 `/`、`/mascot`、`/about` 与 404 测试路径，确认 `scrollWidth` 为 390，metadata 和 skip link 均可检测；临时 QA 文件已删除。
- 使用 Chrome 插件打开 `https://www.radishx.com/`，确认真实 Chrome 中页面标题为 `RadishX - Radish 系列项目矩阵`；DOM / 截图采集阶段出现超时，后续需要复跑完整视觉 smoke。
- 绕过本机失效代理后使用 HTTP 检查确认 `https://www.radishx.com/` 返回 `HTTP/2 200`，Vercel / Cloudflare 响应正常，`x-vercel-cache: HIT`。
- 使用 HTTP 检查确认 `/radish`、`/catalyst`、`/flow`、`/mind`、`/mascot`、`/about` 和 `/abc-test` 均返回 `HTTP/2 200` 并落到 `index.html` fallback。
- 执行 `npm run build` 通过，输出目录为 `dist/`。
- 使用 Browser 与本地开发服务器检查 `1440x900` 和 `390x844` 下的 `/`、四个项目详情页、`/mascot`、`/about` 和 `/abc-test`，确认 `scrollWidth` 等于视口宽度，未发现横向溢出、文本溢出、小点击目标或页面 error console。
- 使用 HTTP 检查确认 `https://www.radishx.com/`、`/radish`、`/catalyst`、`/flow`、`/mind`、`/mascot`、`/about` 和 `/abc-test` 均返回 `200`。
- Browser 截图接口在本轮 `Page.captureScreenshot` 阶段超时；Playwright CLI 包装器 60 秒无输出后中断，线上截图级视觉 smoke 留待浏览器会话稳定后补跑。
- 使用 HTTP 检查确认 `https://radishx.com/` 与 `https://radishx.com/about` 均返回 `200`；`https://www.radishx.com/` 和 `https://www.radishx.com/about` 均返回 `307` 并跳转到对应根域路径。
- 检查 `public/sitemap.xml` 语法和 `public/robots.txt` 内容，并执行 `npm run build` 确认 `dist/` 输出包含 SEO 辅助文件。
- 使用 `sips` 生成首批 Web 图片版本，确认大图最长边约束在 1400px，二维码输出为 512px 级别 PNG。
- 执行 `npm run build` 通过，确认新增公开图片不影响 Vite 构建。
- 使用本地 Vite 预览服务检查 6 个新 Web 图片资源，均返回 `200`，内容类型分别为 `image/jpeg` 或 `image/png`。
- 使用 Browser 检查 `1440x900` 与 `390x844` 下的 `/`、`/mascot`、`/about`，确认页面无横向溢出、无页面 error console，Mascot 页与 About 页图片均已实际加载。
- 使用 headless Chrome 渲染临时本地 HTML，生成 `public/images/social/radishx-og-image.png`，并通过 `sips` / `file` 确认为 1200x630 PNG。
- 预览检查 Open Graph 图片，确认标题、说明、项目 chip、域名和萝小白完全体设定图无遮挡、无明显裁切。
- 使用 Browser 与本地开发服务器检查桌面与 390px 移动端的 `/`、四个项目详情页、`/mascot`、`/about` 和 404 测试路径，确认标题层级、交互元素命名、外链 `rel`、触控目标、横向溢出、metadata、skip link 和 `main#main-content` 均符合预期，未发现页面 error console。
- 使用 Browser 验证站内导航从首页跳转到 `/mascot` 后，焦点会转移到 `main#main-content`，滚动位置回到顶部。
- 使用本地 HTTP 检查确认首页、Mascot、About 和分享预览使用的 7 个公开图片资源均返回 `200`。
- 执行 `npm run build` 与 `git diff --check` 通过。
- 使用 `sips` 生成三张项目详情页 Web 图片，确认输出尺寸分别为 Radish `1024x1024`、RadishCatalyst `1600x900`、RadishFlow `1600x900`。
- 执行 `npm run build` 通过，确认新增 `ProjectVisual` 数据和项目图片接入不破坏 TypeScript / Vite 构建。
- 使用本地 Vite 服务检查三张项目图片资源，均返回 `200` 和 `image/jpeg`。
- 使用 Browser 检查 `/radish`、`/catalyst`、`/flow`、`/mind` 在 `1440x900` 与 `390x844` 下的项目 Hero、媒体框、按钮、chip 和说明文本，确认 `scrollWidth` 等于视口宽度，无元素右侧溢出，三张新图均加载成功，`/mind` 保持图形化占位，页面 error console 为空。
- 使用 Chrome headless 生成 `/flow` 桌面截图和 `/catalyst` 移动长截图作为备用视觉抽查；桌面截图确认 Flow UI baseline 展示正常，移动端截图暴露 chip 行裁切风险后已补小屏 chip 换行规则，并用 Browser 真实 `390x844` DOM smoke 复核通过。
- 使用 Browser 检查 `/mind` 在 `1440x900` 与 `390x844` 下的 `Protocol map`，确认 4 个协议 / 评测节点完整，`scrollWidth` 等于视口宽度，无关键元素横向或高度溢出，页面 error console 为空。
- 执行 `npm run build` 与 `git diff --check` 通过，确认 RadishMind 代码内视觉不破坏 TypeScript / Vite 构建。

## 风险与未完成项

- 首页主视觉尚未最终选图。
- RadishMind 仍缺项目自有公开图片素材；四个项目都尚未确认独立稳定 Logo 或可公开视频素材。
- RadishCatalyst 当前接入的是概念图，不是实机截图；后续如果有真实 gameplay 或 trailer，需要重新审核来源、字幕、水印和移动端加载策略。
- RadishFlow 只展示历史 UI baseline，并已标注 `Archived`；后续不应新增会暗示继续公开维护或产品化承诺的文案。
- “萝小白”首批公开素材已审核并接入；站姿图、表情拆分、活动图和下载口径仍需后续审核。
- 旧可爱Q版设定图不再推荐用于官网公开页面；如需彻底从 GitHub 历史移除，需要单独执行历史清理流程。
- Pencil 设计稿当前保留首页桌面版 v1、首页移动版、项目详情模板、Mascot 页和 About 页；项目详情模板、Mascot 页和 About 页尚未补独立移动版设计稿。
- 首页主视觉仍未最终选图，当前 React 首版使用图形化星图、favicon 临时 Logo 和素材状态槽推进。
- Mascot 页和 About 页已接入首批确认图片；后续新增图片仍需先走审核和 Web 优化流程。
- 需要继续根据 Pencil 设计稿补齐更精细的视觉层级、素材接入和页面动效。
- Browser / Chrome 本轮截图采集链路仍不稳定，线上截图级桌面和移动端视觉 smoke 需要在浏览器会话稳定后复跑。
- 本机 shell 当前存在失效的 `127.0.0.1:10808` 代理配置，后续直接 HTTP 检查要先确认代理状态。

## 后续事项（2026-06-15 起）

1. 在 Browser / Chrome 会话稳定后补跑线上截图级视觉 smoke，覆盖桌面视口和 390px 移动视口下的 `/`、四个项目详情页、`/mascot`、`/about` 与 404 测试路径。
2. 继续评估四个项目是否需要独立 Logo、真实截图或视频素材；RadishMind 后续优先补自有 Logo 或 Console 截图素材审核。
3. 首页首屏主视觉确认后，再评估是否替换当前 Open Graph 分享预览图。
