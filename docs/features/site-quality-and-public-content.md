# 官网质量与对外表达收口

状态：文档目标已建立，代码、页面和部署改动未实施
最后更新：2026-09-06

## 目标

在现有技术栈、灰玉品牌方向和静态官网边界内，提高内容准确性、访客理解效率和验证可靠性。下一轮工作优先修正已有问题，再整理公开文案和评估 SEO 输出方式，不以新增页面或视觉效果作为完成指标。

访客应能回答三个问题：项目做什么、现在能使用或查看什么、下一步去哪里。内容是否新鲜通过核验日期和依据判断，不把官网文档更新时间当作兄弟项目进展证明。

## 背景与证据边界

2026-09-06 综合审阅基于当前仓库源码、文档、构建产物及既有验收截图。生产构建、本地发布检查、协作入口检查和差异检查通过；没有重跑真实浏览器、线上 HTTP、外部项目链接或兄弟项目状态核验。

历史截图只支撑对应日期的表现，不能证明当前页面或线上版本无问题。审阅过程和测量结果见 [2026-W36 开发日志](../devlogs/2026-W36.md)。

## 后续产品核验补充

2026-09-06 随后按所有者要求完成十个本地兄弟仓库的产品文档核验，结果与来源统一见 [产品矩阵同步与扩展](product-matrix-refresh.md)。前节“未核验兄弟项目”仅描述首次综合审阅；本地来源补齐不等于外部链接、产品运行或线上发布已复验。公开文案批次优先消费新矩阵事实，避免沿用旧五项目阶段。

## 范围与实施顺序

| 批次 | 范围 | 进入实施前需要明确 |
| --- | --- | --- |
| A：质量修正 | 状态一致性、主题色、焦点、检查遗漏、小字可读性、联系方式 | 具体修复范围与定向验证；涉及样式时复核桌面与移动 |
| B：公开文案 | 首页项目身份区、项目详情、Mascot、About 的信息层级 | 可审阅文案；如改变页面结构或布局，先确认设计范围 |
| C：SEO 与验证 | 静态 HTML 输出评估、路由和资源检查、可重复浏览器验证 | 输出和 fallback 方案、依赖影响、回退方式；独立确认后实施 |
| D：维护性 | 无引用实现清理、样式职责拆分、内容核验记录 | 真实引用与影响范围；不顺带重做页面或引入组件库 |

本轮用户授权为完善项目文档。本文件建立后续实施依据，不表示静态 HTML 方案、运行时变更、依赖安装、长期服务、CI 远程配置或部署已经获准执行。具体授权继续按 [Agent 协作规则](../development/agent-collaboration.md) 判断。

## 待处理问题

以下条目截至本文件更新时均未修复；完成后逐项补充提交、验证和剩余限制。

| 编号 | 问题与证据 | 建议处理与验收 |
| --- | --- | --- |
| Q01 | `src/app/metadata.ts` 固定写入浅色 `theme-color`，与 `theme.ts` 的主题逻辑竞争；定向函数执行已确认覆盖 | 主题色由主题逻辑统一管理；验证深色直达、换页、锚点和跟随系统切换 |
| Q02 | Flow 数据为 `Archived / Paused`，`ProjectPage.tsx` 的 Future Domain 信息条却固定为 `Coming Soon` | 从真实项目状态生成一致文案；首页、详情、About 均不暗示暂停项目即将上线 |
| Q03 | `App.tsx` 在找到 hash 目标后提前返回，跳过后续换页焦点逻辑；属源码发现，尚未浏览器复现 | 验证跨页锚点、同页锚点、前进后退与 skip link；滚动到目标时焦点位置也应合理 |
| Q04 | `check-local-release.mjs` 的 sitemap 必检列表遗漏 `/lex`；当前 sitemap 本身包含此路径 | 补齐覆盖，并验证缺失任一正式路由会失败；后续收敛路由、sitemap 与检查清单的数据来源 |
| Q05 | 首页状态和域名使用 10–11px muted 文字；`#8d7b6c` 对常用浅底的计算对比度不足普通小字 4.5:1 | 先复核实际背景和信息用途，调整站点文字 token 或具体映射；覆盖明暗主题，不自动扩大为整个 family-ui 改色 |
| Q06 | QQ 联系入口为无号码参数的 `https://qm.qq.com/`，链接本身不能指定该联系号 | 核实有效联系入口，或采用复制号码动作；动作名称与实际结果一致 |
| Q07 | HTML 初始 canonical、title、OG 均为首页，项目 metadata 依赖 JavaScript 改写 | 按下文评估构建期页面输出；原始 HTML 与浏览器最终 metadata 应一致 |
| Q08 | HTTP smoke 只检查 root / module 等 HTML 外壳，不能证明页面内容和交互正确 | 保留传输层检查，增加路由身份、404、资源有效性及关键浏览器行为验证 |
| Q09 | `home-v1.css` 同时覆盖共享 Header；`ProjectBand.tsx`、`homeVisuals.ts` 未发现其他源码引用，旧首页选择器仍有残留 | 先核实动态引用和共享选择器，再移除无用实现、按职责整理样式；不以行数作为拆分唯一依据 |

## 公开内容整理

### 页面信息优先级

| 页面 | 访客优先信息 | 调整方向 |
| --- | --- | --- |
| 首页 | 项目用途、当前状态、站内介绍入口 | 项目身份区评估使用现有短定位；未来域名降为辅助信息，不恢复解释布局的长副标题 |
| 项目详情 | 解决的问题、适用场景、已具备能力、可用入口 | 区分已实现能力与长期方向；压缩模板说明、站内路径和重复素材审核字段 |
| Mascot | 角色介绍、形态特点、精选展示、使用说明 | 审核过程、来源格位和下一素材需求保留在治理文档；必要授权提示集中展示 |
| About | 项目所有者确认的介绍、创作动机、关注与联系路径 | 减少 `No login / API surface`、二维码审核和 Vercel rewrite 等实现说明；不虚构组织背景 |

### 真实状态与展示视觉

- `Source Available` 说明源码可见，不代表可安装、可在线体验或所有规划能力已经实现。
- 暂停维护、概念图、历史 UI、未开放下载等影响访客判断的事实应保留在公开页面，表达简短且位置清楚。
- `NEW POST · 028`、`FORGE ONLINE` 等当前静态展示文案需要复核，避免模拟计数或 HUD 被理解为真实动态和上线状态。可删除非必要数字，或用简短的概念视觉说明消除歧义。
- “本站展示”描述使用范围，不表示访问受限；首批单张表情的术语解释以 [素材治理](../assets/materials.md) 为准。
- 公开页面不要求重复列出素材审批步骤、未来需要什么素材或代码如何实现。压缩说明不改变授权和素材准入条件。

## 内容核验方式

内容维护独立于功能扩展，不必等待新 Logo 或新截图。开始更新项目状态、公开入口或发布内容时，按项目核实：

| 字段 | 记录内容 |
| --- | --- |
| 核验对象 | 项目定位、维护状态、能力或具体入口 |
| 核验日期 | 实际完成该项核验的 Asia/Shanghai 日期 |
| 依据 | 所有者确认，或正式 README / 当前规划 / 发布说明的链接与版本、提交或日期 |
| 官网表达 | 短定位、当前状态、可用动作以及必要限制 |
| 未确认项 | 明确标为未核验，不从旧官网文案推断今天仍然成立 |

暂可将核验记录写入对应 feature 或开发日志；是否增加数据层字段随实际内容更新决定。本轮不伪造五项目的核验日期，不自动建立定时抓取、监控或统计服务。

## SEO 输出评估

当前为 Vite 构建静态资源、单一 `index.html` 入口及客户端 History API 路由。八个正式页面没有独立 HTML 正文；未知路径按现有 fallback 返回入口，客户端再设置 `noindex`。

建议优先评估构建时为正式路由输出正文和独立 metadata，保持静态交付。方案需明确：

1. 页面数据、路由、sitemap 和 metadata 如何共享事实，避免生成后再次分叉。
2. 正常直达、刷新、尾斜杠、站内跳转及未知路径的输出与 HTTP 行为。
3. 原始 HTML 的 title、description、canonical、OG / Twitter 信息与浏览器最终状态一致。
4. 404 页面与资源路径的区分，缺失图片 / JS / CSS 不落到成功 HTML 外壳。
5. 是否增加依赖、如何构建、如何保持现有交互，以及如何回退。

本节没有选定新框架、预渲染工具或新的 Vercel 配置。当前客户端 `noindex` 是已有缓解，不能把未知路径返回成功外壳作为未来方案的验收目标；本轮也没有证据证明线上收录已受损。

参考：[Google JavaScript SEO](https://developers.google.com/search/docs/crawling-indexing/javascript/javascript-seo-basics)、[W3C 普通文字对比度](https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html)。

## 验证与完成标准

- 每个批次独立记录范围、实现结果和必要验证，不用历史全站截图或构建成功替代新改动的验证。
- 代码变化执行 `npm run build`；构建输出变化执行 `npm run check:local-release`；HTTP 与浏览器检查按 [开发规范](../development/standards.md) 执行。
- 页面改动至少覆盖 `1440x900` 和 `390x844`，共享主题或响应式改动再覆盖受影响主题与断点。
- 可复用浏览器脚本应进入受版本管理的验证目录；截图、运行汇总继续放 `output/`。不要把只存在于本地 artifact 的脚本称为仓库可复跑入口。
- 性能优化以真实加载证据为依据；本次包大小不等同于 LCP、CLS 或弱网性能。没有实际需要时不拆分大量小 chunk，不批量转码原图。
- 收口时逐项确认：访客能辨认用途、状态、可用入口；已选入批次的问题完成验证；未选方案明确保留为后续；本地完成与线上发布分别记录。

## 不做

- 不重做整个品牌、不一次性扩展暗色精确稿或其他页面视觉。
- 不新增业务后台、CMS、统计服务、下载能力或 seasonal 页面。
- 不自动迁入兄弟项目代码、截图、Logo 或视频。
- 不因为文档目标建立就安装依赖、启动长期服务、推送、部署或修改域名。
