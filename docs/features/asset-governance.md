# 素材治理目标

状态：持续执行，Mascot 单张拆分准备清单、首批人工框选复核准备、授权 / 页面接入范围确认、seasonal 活动图评估与项目视觉素材准入清单已完成
最后更新：2026-06-18

## 目标

建立 RadishX 官网素材从候选、审核、Web 优化、页面接入到后续替换的稳定流程，避免未经审核图片、外部参考图或不清晰版权素材进入正式页面。

## 背景

RadishX 已接入首批 Mascot / About 图片和三个项目详情页 Web 图。首页首屏已使用已审核可爱Q版站姿图作为 Radish Orbit 中心视觉锚点。Mascot 表情拆分、缩略图命名和下载政策已完成当前阶段评估，并已建立单张拆分准备清单；项目 Logo、真实截图、视频和 RadishMind 自有视觉的准入字段已拆分到独立目标文档，后续仍等待新的正式候选素材。

## 范围

- `assets/` 中候选素材。
- `public/images/` 中已审核公开 Web 资源。
- `docs/assets/materials.md` 中素材清单、审核状态和版权口径。
- 页面数据层中图片来源、用途、使用边界和后续需求。

## 不做

- 不覆盖原始素材。
- 不批量裁切、压缩、重采样或转换格式，除非用户确认。
- 不修改兄弟项目素材或代码。
- 不把外部产品参考截图作为官网项目素材。
- 不提供下载入口或自由复用声明，除非版权口径单独确认。

## 准入规则

- 正式页面使用前，素材必须先记录到 `docs/assets/materials.md`。
- 进入 `public/images/` 的图片必须是 Web 优化版本，不直接使用原始候选图。
- 每个项目详情页素材必须说明 `Asset source`、`Usage boundary` 和 `Next material need`。
- 概念图、历史 UI baseline、代码内视觉和真实截图必须在页面文案中区分。
- 视频接入前必须确认来源、压缩、字幕 / 水印、移动端降级图和加载策略。

## 开发任务

- 后续新增素材时先更新审核清单，再生成 Web 版本。
- 为项目 Logo、截图和视频分别记录来源、用途、风险和接入结论。
- 四项目正式 Logo、截图、视频或 RadishMind 自有视觉出现候选时，先按 [项目视觉素材准入准备](project-visual-asset-readiness.md) 补齐字段，再判断是否生成 Web 版本。
- Mascot 新图进入页面前同步更新 Mascot 目标文档。
- Mascot 单张表情如未来进入页面，先按准备清单完成逐格审核、命名确认、授权边界、撤回策略和下载口径复核，再生成单张 Web 文件。
- 首页主视觉变化后，记录是否影响 Open Graph 图。

## 当前复核

- 2026-06-15 已完成四个兄弟项目的项目 Logo、真实截图和公开视频素材二轮只读复核。
- 本轮没有发现新增或修改的媒体文件，也没有发现可直接公开的视频素材。
- Radish 继续使用已接入代表图；RadishCatalyst 继续只使用概念视觉；RadishFlow 继续保留 archived / 历史 UI 口径；RadishMind 继续使用代码内 `Evaluation loop`，不接入外部产品参考截图。
- 四个项目的独立稳定 Logo 仍等待未来单独确认；确认前继续使用代码内文字 / CSS 临时项目标识。
- 2026-06-17 已建立四项目视觉素材准入清单，覆盖 Logo、截图、视频、key visual、diagram、自有视觉、替换范围、降级策略和授权备注；当前仍不新增项目素材、不替换临时标识。
- 2026-06-15 已将可爱Q版表情格和 4 张贴纸横图生成 Web 版本并接入 Mascot Gallery；当前只做整图预览，不拆分单张，不提供下载入口。
- 2026-06-15 已完成 Mascot 单张拆分、缩略图命名和下载政策评估；当前继续整图预览，不生成单张文件、不生成缩略图、不开放下载。
- 2026-06-15 已完成 `assets/avatars/seasonal/` 6 张节日活动图评估；全部只作为短期活动 Banner、节日彩蛋或运营内容候选，不进入长期 Gallery、默认主视觉或当前实现。
- 2026-06-17 已建立 Mascot 单张表情拆分准备清单，字段覆盖 `id`、来源图、来源格位、中文标签、英文情绪、slug、公开状态、使用场景、授权边界、撤回策略、计划 Web / 缩略图路径、下载口径和复核备注；当前没有生成单张 Web 文件或缩略图。
- 首批候选只从 `assets/avatars/child/radish-child-expression-sheet-grid.png` 选取 `开心`、`生气`、`震惊`、`得意`、`期待`、`疑问`、`无语`、`OK`、`加油`、`再见` 10 个基础情绪 / 动作格；4 张贴纸横图继续作为二批复核来源。
- 2026-06-18 已完成首批 10 个候选的人工框选复核准备，按原始 `1024x1536` PNG 的 4 列 6 行记录整格 `256x256` 计划裁切框、保留要素和复核结论；全部继续保持 `candidate-only`，不生成单张 Web 文件、不生成缩略图、不开放下载。
- 2026-06-18 已完成首批单张表情授权与页面接入范围确认：当前只允许作为 RadishX 官网内部轻量展示候选，不作为下载素材、社交贴纸包、项目 Logo、首页主视觉或外部复用素材；生成单张 Web 文件前仍需项目所有者确认最终公开展示授权。

## Mascot 单张表情准入

当前准备清单是审核入口，不是文件生成授权。后续如果进入单张展示或文件包评估，按以下顺序执行：

1. 在 `docs/assets/materials.md` 和 `docs/features/mascot-page.md` 中确认候选条目的 `public_status`、`usage_scenario`、`usage_boundary`、`license_scope`、`withdrawal_policy` 和 `download_policy`。
2. 对候选格做人工框选复核，确认主体完整、外装完整、表情符号和中文标签未被裁掉，且无身体拆解或性感化风险。
3. 确认命名：表情格使用 `radish-child-expression-<slug>-web.jpg`；缩略图仅在页面或文件包真实需要时使用 `radish-child-expression-<slug>-thumb.jpg`。
4. 生成文件前再次确认授权来源、允许 / 禁止使用场景、归属说明和撤回策略；未确认前不写入 `public/images/mascot/expressions/`。
5. 页面接入前同步更新数据层、页面文案和完成标准，继续保持下载入口关闭。

2026-06-18 首批候选计划框选结论：

| id | source_cell | planned_crop_box_original_px | 复核结论 |
| --- | --- | --- | --- |
| `rx-expression-001` | `R1C1` | `x=0,y=0,w=256,h=256` | 可进入授权确认；保留双手、黄色强调线和中文标签。 |
| `rx-expression-002` | `R1C2` | `x=256,y=0,w=256,h=256` | 可进入授权确认；保留怒气符号、蒸汽和抱臂姿态。 |
| `rx-expression-003` | `R1C3` | `x=512,y=0,w=256,h=256` | 可进入授权确认；保留左侧爆点、双手和张口表情。 |
| `rx-expression-004` | `R2C3` | `x=512,y=256,w=256,h=256` | 可进入授权确认；低风险优先候选。 |
| `rx-expression-005` | `R2C4` | `x=768,y=256,w=256,h=256` | 可进入授权确认；文案不写成上线承诺。 |
| `rx-expression-006` | `R3C2` | `x=256,y=512,w=256,h=256` | 可进入授权确认；保留右上问号。 |
| `rx-expression-007` | `R3C3` | `x=512,y=512,w=256,h=256` | 可进入授权确认；使用语境不得嘲讽用户。 |
| `rx-expression-008` | `R5C2` | `x=256,y=1024,w=256,h=256` | 可进入授权确认；不作为独立授权图标。 |
| `rx-expression-009` | `R5C3` | `x=512,y=1024,w=256,h=256` | 可进入授权确认；不用于商业承诺或进度承诺。 |
| `rx-expression-010` | `R6C4` | `x=768,y=1280,w=256,h=256` | 可进入授权确认；适合页尾或 404 辅助候选。 |

2026-06-18 授权与页面接入范围：

| 范围 | 当前结论 |
| --- | --- |
| 授权来源 | RadishX 仓库内原创候选素材；仓库公开展示不等于授予素材复用、改作、下载、商用或再分发权利。 |
| 允许页面位置 | `/mascot` 单张表情预览区候选、404 辅助视觉、FAQ / 状态反馈、开发日志辅助视觉。 |
| 禁止页面位置 | 首页首屏主视觉、四项目 Logo、项目图标、下载区、素材包入口、社交贴纸包入口、商业宣传授权页。 |
| 文件生成边界 | 项目所有者确认公开展示授权后才可生成 `public/images/mascot/expressions/*-web.jpg`；缩略图只在页面性能或网格浏览真实需要时生成。 |
| 页面文案边界 | 只能写“展示候选”“官网内部展示”“待审核”，不能写“免费下载”“自由使用”“贴纸包”“素材 API”。 |
| 撤回策略 | 撤回时同步移除公开文件、数据引用、页面入口、文档清单和任何下载 / 复用暗示；线上已发布时随下一次发布撤下。 |

准入字段：

| 字段 | 准入要求 |
| --- | --- |
| `id` | 清单稳定编号，不复用已撤回编号。 |
| `source_sheet` / `source_cell` | 指向原始候选图和明确格位，不能只记录 Web 整图预览。 |
| `label` / `emotion` / `slug` | 中文标签、英文语义和文件名片段必须一致；冲突时先改清单。 |
| `public_status` | 生成前为 `candidate-only`；通过裁切和授权复核后才可改为待接入状态。 |
| `usage_scenario` | 只允许官网内部轻量插图、状态反馈、FAQ、开发日志辅助视觉或页面彩蛋候选。 |
| `usage_boundary` | 保持完整外装、非性感化、无身体拆解；不能裁掉关键手势、脸部、符号或标签。 |
| `license_scope` | 当前不开放对外二次使用、商用复用、社交贴纸包、下载或再分发。 |
| `withdrawal_policy` | 撤回时同步移除文件、数据引用、页面入口、文档清单和下载说明。 |
| `planned_web_path` / `planned_thumb_path` | 仅作为计划路径；生成前不得在页面中引用。 |
| `download_policy` | 当前固定为 `closed`，不提供 ZIP、单图下载、素材 API 或自由复用声明。 |

## Seasonal 活动图准入

`assets/avatars/seasonal/` 当前不直接进入官网长期页面。未来如需使用，必须先确认：

- 活动目标：新春祝福、元旦彩蛋、跨年运营、年末回顾等具体场景。
- 活动窗口：明确上线日期、下线日期和过期后处理方式。
- 页面位置：首页临时活动 band、`/mascot` 顶部活动条、节日彩蛋、社交运营图或活动归档；不能默认进入长期 Gallery 或常驻 Hero。
- 文案边界：只表达节日问候或一次性活动，不承诺项目上线，不声明素材可自由复用，不开放下载。
- 授权边界：确认生成来源、字体 / 文案使用权、公开运营传播范围、是否允许二次裁切和撤回策略。

当前分类：

| 分类 | 文件 | 使用边界 | 当前结论 |
| --- | --- | --- | --- |
| 活动 Banner | `radish-seasonal-horse-year-gate.png`、`radish-seasonal-horse-year-scene.png`、`radish-seasonal-horse-year-poster-red.png` | 只适合 2026 马年新春短期活动位，当前新春窗口已过。 | 不进入当前实现。 |
| 节日彩蛋 | `radish-seasonal-new-year-2026-lantern.png` | 只适合 2026 元旦短期彩蛋或轻量装饰，当前窗口已过。 | 不进入当前实现。 |
| 运营内容候选 | `radish-seasonal-new-year-2025-to-2026.png`、`radish-seasonal-sakura-goodbye-2025.png` | 只适合跨年 / 年末回顾类运营内容，不能作为当前活跃页面视觉。 | 不进入当前实现。 |

## 验证

- `npm run build`
- 本地检查新增 `public/images/` 资源返回 `200`。
- 视觉改动时检查桌面和 390px 移动端。
- 检查图片尺寸、格式、`alt`、加载状态和主体裁切。
- 执行 `git diff --check`。
- 纯文档复核类变更执行 `git diff --check`，不要求启动本地浏览器 smoke。

## 完成标准

- 新素材有明确来源、用途、风险、结论和文件路径。
- 原始素材保留在 `assets/`，公开版本进入 `public/images/`。
- 页面、素材文档和目标文档口径一致。
- 未审核素材不进入正式页面。

## 后续事项

- Mascot 单张表情拆分只有在确认拆分清单、首批候选和授权边界后再启动文件生成。
- 首批 10 个表情格已具备计划框选记录和页面接入范围；下一步先决定是否生成单张 Web 文件、是否新增 `/mascot` 单张预览区，再同步数据层、页面文案和设计源。
- Mascot seasonal 图后续只有在确认具体活动目标、上线 / 下线时间、页面位置、文案和授权后，才生成 Web 版本并进入短期实现。
- 四个项目稳定 Logo、真实截图和可公开视频素材；只有出现新的候选来源时再重新进入审核和 Web 优化。
- 四项目新候选素材重新审核时，使用 `docs/features/project-visual-asset-readiness.md` 中的字段和接入顺序。
- RadishMind 自有公开视觉。
