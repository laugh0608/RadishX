# RadishX 协作约定

本文件为 RadishX 仓库中的 AI 协作者与人工协作者提供启动级长期约束。

## 定位与优先级

- `docs/` 是本仓库正式文档真相源；本文件只保留任何任务开始时都必须知道的长期约束，不承载当前阶段、临时门禁、历史记录或专题实现细节。
- 当前阶段、优先级、停止线和“当前不做”以 `docs/planning/current.md` 为准；页面、功能、视觉、素材、验证和部署细则以对应专题文档为准。
- 项目所有者在当前任务中的明确要求优先于仓库默认流程；若要求会改变架构、规则、接口、依赖、部署或运行时边界，仍应先说明影响并确认范围。
- 只读取当前任务需要的文档；需要追溯历史事实时，再按需读取 `docs/devlogs/` 或已完成的 feature 文档。

## 称呼与语言

- 对话开始或结束总结时，请称呼项目所有者为 `萝卜SAMA`。
- 默认使用中文进行讨论、说明、提交总结和开发日志记录。
- 代码、命令、路径、配置键、类型名、接口名、URL 和外部产品名保留原文。
- 新增文档正文默认使用中文；文件名、目录名和稳定锚点优先使用英文。

## 协作原则

- 开始任务前先检查工作区状态，并读取任务路由指向的必要文档。
- 涉及架构、规则、接口、依赖、部署、运行时行为或范围不清的改动前，先说明方案并等待确认。
- 用户明确要求直接修改且范围清晰、风险可控时，直接实施。
- 小规模、低风险、需求明确的文档、配置或素材整理类变更可以直接实施。
- 需求存在多种合理解释且会明显改变结果、风险或范围时，先澄清再修改。
- 优先处理根因、长期维护性、系统一致性和可验证性；不把临时兼容、层层兜底或无真实收益的抽象作为默认方案。
- 长期功能、重要页面、跨模块能力、素材替换和阶段性目标进入实现前，先确认对应 `docs/features/*.md` 存在且边界清楚。
- 每完成一个可分割步骤，执行与风险和影响面相匹配的必要验证。
- 更完整的任务推进、文档分层和授权细则见 `docs/development/agent-collaboration.md`。

## 必须单独授权的操作

- 安装或更新依赖，例如 `npm install`、`npm update`、`npm create`。
- 启动长期运行进程，例如 `npm run dev`、`npm run preview`。
- 创建或修改远程仓库、推送远端分支、配置 Vercel 项目、部署站点或修改 DNS。
- 引入新的外部图片、字体、依赖库或第三方服务。
- 批量压缩、裁切、重采样、转换或覆盖原始素材。
- 授权只覆盖当前任务中已经说明的命令、目标和影响；范围发生实质变化时重新确认。
- 仓库内代码和文档读写、构建、类型检查、静态验证、只读 Git 操作与明确的小规模本地提交可以直接执行。

## 任务文档路由

| 任务 | 优先读取 |
| --- | --- |
| 当前阶段、优先级、下一步 | [当前规划](docs/planning/current.md) |
| Agent 协作、执行授权、文档分层 | [Agent 协作与执行规则](docs/development/agent-collaboration.md) |
| 页面、功能、素材替换或发布目标 | [功能与目标文档](docs/features/README.md)及对应 feature 文档 |
| 工程结构、前端、响应式、验证和提交 | [开发规范](docs/development/standards.md) |
| 视觉方向、Pencil 和 family-ui | [视觉规范](docs/design/visual-guidelines.md)、[family-ui](docs/design/family-ui/README.md) |
| 素材准入、授权、转换和公开使用 | [素材治理](docs/assets/materials.md) |
| Vercel、域名、跳转和发布边界 | [Vercel 与域名](docs/deployment/vercel.md) |
| 已完成结果、验证证据和遗留风险 | [开发日志](docs/devlogs/README.md) |

## 项目长期边界

- RadishX 是 Radish 系列项目的官网与统一入口，使用 Vite、React、TypeScript 构建静态多页面站点，并通过 GitHub 与 Vercel 交付。
- `radishx.com` 是 canonical 主域，`www.radishx.com` 是兼容跳转入口；未来项目子域不属于本官网路由或 rewrite。
- 官网负责项目矩阵、项目介绍、Mascot 与 About 等展示内容，不承载五个项目的实际业务、登录、后台、数据库或服务端 API。
- PC 与移动端都是一等目标；涉及页面、布局或视觉的首版实现必须同时考虑响应式与可访问性。
- 官网视觉继承 `docs/design/family-ui/` 的 Brand 展示面规则，具体风格、token 和素材边界按视觉与素材专题执行。

## 实现红线

- 不跨工作区修改兄弟项目，不把兄弟项目代码整包迁入当前仓库。
- 不执行破坏性 Git 操作，不覆盖或丢弃不属于当前任务的现有改动。
- `.pen` 设计文件只能通过 Pencil 读写；历史设计基线的保留与当前设计源选择以视觉专题为准。
- 候选素材进入正式页面前必须按素材治理文档完成来源、授权和使用范围确认；不得覆盖原始素材。
- 实现样式优先使用 `--rx-*` 语义 token，不在 JSX 中扩散硬编码颜色、阴影和复杂布局值。
- 新增依赖、组件或抽象前先确认真实收益；能用语义 HTML、CSS 和现有组件完成时不扩大技术面。

## 验证与文档

- 验证入口以 `package.json` scripts 和 `docs/development/standards.md` 为准；不要求每次改动都执行与影响面无关的全量检查。
- 涉及视觉、布局或响应式的改动，至少检查一个桌面视口和一个移动视口。
- 修改页面、功能、视觉、素材、部署或验证规则时，更新对应 `docs/` 真相源；阶段状态和临时门禁只更新当前规划或专题文档。
- 重要推进、关键决策、验证结果和遗留风险记录到 `docs/devlogs/YYYY-Www.md`，日期使用 Asia/Shanghai（UTC+8）。

## Git 约束

- 使用简洁明确的 Conventional Commits 风格，按主题拆分代码与文档改动。
- 提交前确认必要验证已经执行，且工作区没有误改兄弟项目或混入无关文件。
- 使用当前用户 Git 身份，不添加任何 AI 协作者署名。

## 入口文件维护

- `AGENTS.md` 与 `CLAUDE.md` 只在跨任务、跨阶段且必须启动即生效的长期约束发生变化时修改。
- 阶段状态、临时门禁、“当前不做”、具体设计源状态和批次验证事实不得复制回根入口。
- 两个文件除首段入口说明外必须逐字同步；修改任一文件时同步另一份，并执行 `npm run check:agent-guides`。
- 详细维护判定与内容归位规则见 `docs/development/agent-collaboration.md`。
